# SPEC — 클립별 오버라이드 · Reuse · 프롬포트 프리셋 자산 · 미디어 슬롯 · Ollama 제거

노드 쪽(ComfyUI-TJ_NODE_STUDIO_ONE)에서 2026-08-28 ~ 08-31 사이에 구현·검증을 마친
변경 묶음이다. 웹 버전도 같은 동작을 갖도록 이식한다. 각 항목은 노드에서 브라우저로
실제 동작을 확인한 것만 적었고, 확인 방법과 **걸렸던 함정**도 같이 남긴다. 함정 쪽이
이식할 때 더 쓸모 있을 것이다.

전제: 이 문서는 `SPEC_MINIMAX_H3_PRESETS.md` 이후의 변경분이다. 파이프라인 프리셋
(왼쪽 패널의 Save / Setting)은 그 문서에 있고, 여기 §7의 **프롬포트 세트**는 그것과 다른
기능이다 — Prompt Edit 상단의 Load / Save / Delete 쪽이다.

§1~§6은 첫 작성분, §7~§13은 그 이후 추가·수정분이다.

---

## 1. 클립별 레퍼런스 오버라이드 (핵심)

### 왜 필요한가

이 노드의 장점은 멀티 프롬포트를 한 번에 큐에 넣고 루프로 돌리는 것이다. 그런데 레퍼런스
이미지가 전역 하나뿐이면, 샷마다 그림이 달라져야 하는 순간 그 장점이 사라진다. 이미지를
바꾸고 클립 하나 돌리고 다시 바꾸는 단일 프로세스로 되돌아간다.

반대로 전부 클립별로만 두면 공통 레퍼런스를 매 클립에 다시 넣어야 한다. 그래서 **명시적
체크박스**로 고른다.

### 동작

Prompt Edit의 기존 첨부 영역이 곧 오버라이드 저장소다. 새 UI 구조를 만들지 않고 읽고 쓰는
대상만 바뀐다.

- 체크 해제(기본): `state.refImages` 등 **공통** 세트를 읽고 쓴다
- 체크: `prompts[i].refImages` 등 **그 클립 전용** 세트를 읽고 쓴다
- 처음 체크할 때 공통 세트를 복사해 씨딩한다 (빈 칸 두 개를 주지 않기 위해)

**all-or-nothing이다.** 체크하면 그 클립은 자기 세트만 쓴다. 자산별로 공통에 폴백하지
않는다. 눈에 보이지 않는 혼합이야말로 잘못된 렌더를 늦게 발견하게 만드는 원인이다.

오버라이드가 덮는 범위: 레퍼런스 이미지, first/last frame, 레퍼런스 비디오, 레퍼런스 오디오,
**그리고 헤더와 사운드/뮤직 테일**(§2). 사운드 락은 전역으로 남는다.

### 단일 리졸버를 둘 것

노드에서는 `clipAssets(state, i)` 하나가 "이 클립이 실제로 무엇으로 렌더되는가"를 정한다.
렌더 루프, 에디터, 메타데이터가 전부 이 함수를 통과한다. 세 곳이 각자 판단하면 서로
어긋나고, 그 어긋남은 렌더가 끝난 뒤에야 드러난다.

```js
export function clipAssets(state, i) {
  const p = (state.prompts || [])[i];
  if (promptOverrides(p)) {
    return { own: true, refImages: (p.refImages || []).filter(Boolean),
      refImagesMp: p.refImagesMp || [], firstFrame: p.firstFrame || "",
      lastFrame: p.lastFrame || "", refVideos: p.refVideos || [], refAudios: p.refAudios || [] };
  }
  return { own: false, refImages: (state.refImages || []).filter(Boolean), /* ... */ };
}
```

### 화면 표시

첨부 영역 머리말이 상태를 말해야 한다. 체크 해제 상태에서 거기를 편집하면 **다른 모든
클립이 공유하는 공통 세트**가 바뀌기 때문이다. 의도된 동작이지만 화면에 적혀 있어야 한다.

- 해제: `Common (shared by all clips)`
- 체크: `This clip only` (브랜드 컬러)

### 프롬포트 엔트리 형태

```
{ text, firstFrame, enabled, override, refImages, refImagesMp,
  lastFrame, refVideos, refAudios, header, footer }
```

### 확인함

클립1 오버라이드 켜고 이미지 3장, 클립2는 공통 1장 유지 — 서로 침범하지 않음.

---

## 2. 헤더와 사운드/뮤직도 오버라이드를 따라간다

**이게 처음 구현에서 빠졌던 부분이고, 반드시 같이 가야 한다.**

헤더는 비주얼 스타일·그레이드·오프닝 구도를 정하고, 테일은 앰비언스와 스코어를 정한다.
둘 다 *레퍼런스 이미지가 세우는 장면*을 서술한다. 그래서 클립이 이미지를 다른 세트로
바꿨는데 공통 헤더/테일을 그대로 쓰면, 이전 샷의 설정과 음악이 새 장면 위에 덮어써진다.
에러가 나지 않고 조용히 잘못된 렌더가 된다.

이미지와 **같은 all-or-nothing 규칙**을 적용한다. 노드에는 `clipAssets`와 짝이 되는
`clipFraming(state, i)`을 뒀다:

```js
export function clipFraming(state, i) {
  const p = (state.prompts || [])[i];
  if (promptOverrides(p)) return { own: true, header: p.header || "", footer: p.footer || "" };
  return { own: false, header: state.promptHeader || "", footer: state.promptFooter || "" };
}
```

프롬포트 합성이 이걸 통과한다:

```js
const { header, footer } = clipFraming(state, i);
return [header, body, footer, loraTriggers(state), state.promptSuffix]
  .map(s => (s || "").trim()).filter(Boolean).join("\n\n");
```

라벨도 같이 바뀐다: `COMMON — HEADER` ↔ `THIS CLIP — HEADER`,
`COMMON — SOUND / MUSIC` ↔ `THIS CLIP — SOUND / MUSIC`.

클립을 전환하면 두 입력 상자를 **그 클립 기준으로 다시 읽어야 한다**. 노드에서는
`refreshFraming()`을 클립 클릭, 오버라이드 토글, 패널 열기 세 곳에서 부른다.

### 확인함

클립1(오버라이드)에 `CLIP1-OWN-HEADER` / `CLIP1-OWN-SOUND` 입력 →
`composeClipPrompt(st, 0)`은 그걸로 시작하고, `composeClipPrompt(st, 1)`은 공통 헤더로
시작하며 `CLIP1-OWN-SOUND`를 포함하지 않음. 클립 전환 시 라벨과 내용 모두 따라감.

---

## 3. Prompt Edit은 노드 모드에 맞는 인핸스 모드로 열린다

- `generationMode === "t2v"` → `Text → Brief`, 첨부 영역 없음
- Reference / First-Last → `Image → Brief`, 첨부 영역 표시

Reference/F-L에서는 이미 이미지가 붙어 있는데 Text → Brief로 열리면 첨부 영역과 오버라이드
체크박스가 클릭 한 번 뒤에 숨는다.

> 함정: 노드에서 Text only의 상태값은 `"text"`가 아니라 **`"t2v"`** 다. 웹에서 값이
> 다르다면 그쪽 값으로 맞출 것. 이 한 글자 때문에 처음 구현이 동작하지 않았다.

---

## 4. Reuse가 이미지를 복원한다

`metaForVideo()`가 프롬포트·해상도·샘플링·파이프라인 축은 저장하면서
`refImages` / `refImagesMp` / `firstFrameImage`는 저장하지 않았다. 그래서 Reference 모드에서
"이 클립 그대로 다시" 가 실제로는 아무것도 재현하지 못했다.

메타에 추가할 필드: `refImages`, `refImagesMp`, `firstFrameImage`, `lastFrameImage`,
`refVideos`, `refAudios`. `reuseAll()`에서 되돌린다.

**오버라이드보다 먼저 넣어야 한다.** 순서가 뒤바뀌면 복원 불가능한 이미지 세트가
1개에서 N개로 늘어난다.

### 확인함

`refImages`를 빈 배열로 지운 상태에서 갤러리 Reuse → 파일명·MP·레퍼런스 비디오·프롬포트가
전부 복귀.

---

## 5. 업스케일 / 디블러 / 스티치 출력의 메타

- **업스케일·디블러 결과**: 원본 클립의 메타를 그대로 복사한다
- **스티치 결과**: 원본 메타를 복사하되 프롬포트는 각 원본의 합성 프롬포트(헤더+본문+테일)를
  `[Clip N]` 구분자로 이어 붙인다

```js
export function composeStitchedPrompt(clipPrompts) {
  const parts = (clipPrompts || []).map(t => String(t || "").trim()).filter(Boolean);
  if (parts.length <= 1) return parts[0] || "";
  return parts.map((t, i) => `[Clip ${i + 1}]\n${t}`).join("\n\n");
}
```

구분자를 쓰는 이유: 빈 줄만으로는 한 샷이 어디서 끝나고 다음이 시작하는지 읽는 사람도,
나중의 Reuse도 알 수 없다.

**스티치본의 Reuse는 합쳐진 텍스트를 한 칸에 넣지 않는다.** `[Clip N]`으로 잘라
프롬포트1 / 프롬포트2로 **분리 복원**하고, 마커는 제거한다. 헤더/테일은 각 프롬포트 안에
포함된 채로 들어간다.

### 확인함

0.1MP·124f·4스텝으로 2클립 렌더 → 갤러리에서 두 개 선택해 Combine →
`MMH3_full_20260831-031822.json`에 `refImages`, `seed`, `steps: 4`가 복사되고 프롬포트에
`[Clip 1]` `[Clip 2]` 존재(7237자). 그 파일을 Reuse → 프롬포트 2개로 분리, 마커 제거됨.

---

## 6. Ollama 제거, 네이티브 비전만 남김

Image → Brief의 백엔드 둘 중 외부 Ollama 서버를 없애고 ComfyUI 내부 경로만 남긴다.
별도 서버가 필요했고, LLM이 둘이면 클립별 오버라이드 작업이 불필요하게 복잡해진다.

- `state.visionSource` 기본값이 `"ollama"`였다 — **반드시 뒤집을 것**. 안 그러면 기존 저장
  상태가 전부 존재하지 않는 백엔드를 가리킨다
- `llm/ollama_models`, `llm/enhance` 라우트 제거
- UI 표기: `LOCAL ENHANCE (native CLIP)`

> 노드에서는 설정 파일에 `ollama_url` 등 5개 키가 **사문화된 채로 남아 있다**. 읽는 코드가
> 없어 동작에는 영향이 없지만, 웹에서는 마이그레이션 때 지우는 편이 낫다.

내부 변수명 `ollamaImages` / `ollamaImageMode`는 노드에서 아직 그대로다(동작 무관, 정리 예정).

---

## 7. 프롬포트 프리셋이 이미지·비디오·오디오까지 저장/로딩한다

Prompt Edit 상단의 세트 드롭다운(Load / Save / Delete)은 지금까지 **글자만** 저장했다.
Reference 모드에서 그 세트를 다시 불러도 아무것도 재현되지 않는다. 사진과 클립은 프롬포트
세트의 부속물이 아니라 세트 그 자체의 일부다.

### 저장/로딩에 포함되는 것

```
promptHeader, promptFooter, prompts[]        (기존)
generationMode, refTypes
refImages, refImagesMp
firstFrameImage, lastFrameImage
refVideos[], refAudios[]                     (file / start / end / withAudio 전부)
```

`prompts[]`는 **엔트리를 통째로** 복원해야 한다. 기존 로딩 코드는 `text` / `firstFrame` /
`enabled` 세 필드만 골라 담고 `override`와 `refImages`를 버렸다 — 저장은 되는데 로딩에서
사라지던 원인이 이것이다.

### 서버 저장 라우트의 화이트리스트를 반드시 같이 고칠 것

노드 쪽에서 실제로 시간을 잡아먹은 함정이다. 클라이언트에 필드를 추가해도 서버 저장
핸들러가 `promptHeader` / `promptFooter` / `prompts`만 통과시키고 **나머지를 조용히 버렸다.**
클라이언트만 고치고 서버를 보지 않으면 저장된 파일에 자산이 없고, 에러는 나지 않는다.

노드에서는 payload를 `v: 2`로 올리고 위 필드를 전부 기록하게 했다.

### 구버전 세트 보호

자산 필드가 **없는** 세트를 로드할 때는 그 항목들을 건드리지 않는다.

```js
if (Array.isArray(s.refImages)) state.refImages = s.refImages.slice();
```

이 조건이 없으면 이 기능 이전에 저장한 세트를 여는 순간 화면의 이미지가 지워진다.

---

## 8. 없어진 첨부 파일은 고스트 썸네일로 알린다

프리셋이 기억하는 파일은 `input/` 폴더에 있고, 그 폴더는 언제든 정리된다.

**빈 칸으로 두면 안 된다.** 사진 3장을 쓰던 세트인지 0장이던 세트인지 구분할 방법이
사라진다. 그래서 이름은 그대로 남기고, 썸네일 자리에 없다는 사실을 새긴다.

### 표시

- 주황 점선 테두리 + `⚠` + 파일명(작게, 줄바꿈)
- 호버하면 `Missing from the input folder:\n<파일명>` 툴팁
- 번호 배지와 `✕`를 그대로 유지 — 고스트는 조치를 요구하는 표시지 잠긴 칸이 아니다.
  누르면 지워지고, 타일을 클릭하면 다시 채울 수 있다
- 이미지 · 비디오 · 오디오 세 종류 모두 동일

### 조회는 한 번에

파일마다 HEAD를 던지면 로드 한 번에 수십 왕복이 된다. 노드에는 이름 배열을 받아 없는 것만
돌려주는 라우트를 하나 뒀다.

```
POST /tj_shared/input_exists   { names: [...] }  ->  { missing: [...] }
```

이름은 저장된 상태에서 오므로 **경로로 신뢰하지 말 것.** 노드 구현은
`os.path.basename(n) != n`이면 그대로 missing 처리한다.

로드 직후 공통 자산 + 모든 클립의 자산을 모아 한 번 조회하고, 결과를 `state.missingAssets`에
넣은 뒤 다시 그린다. 팝업은 개수와 파일명(4개까지, 나머지는 "and N more")을 알린다.

---

## 9. 브라우저 기본 다이얼로그를 쓰지 말 것

ComfyUI 프론트엔드는 `window.prompt` / `confirm` / `alert`을 **억제한다.** 이걸 기다리는
핸들러는 거기서 멈추고 자기 코드에 도달하지 못한다. 밖에서 보면 버튼이 죽은 것과 구별이
안 되고, 에러도 남지 않는다.

노드에서 이 함정에 **두 번** 걸렸다 — 파이프라인 프리셋의 Rename/Delete, 그리고 프롬포트
세트의 Save/Delete. 두 번째는 "프리셋 저장 기능"을 고치는 작업 중에 발견하고도 방치했다가
저장이 안 되는 채로 넘어갈 뻔했다.

`web/shared/ui_ask.js`로 공용 다이얼로그를 뺐다. `kind: "text" | "confirm"`, 부모 엘리먼트에
마운트하므로 노드 안에 클리핑된다.

**이식 시 웹 프로젝트도 전체 grep 할 것.** 억제되지 않는 환경이라면 그대로 둬도 되지만,
동작 확인 없이 가정하지 말 것.

---

## 10. Prompt Edit 레이아웃

목업을 기준으로 맞춘 결과다. 웹은 레이아웃이 다르므로 치수를 그대로 옮길 필요는 없고,
**어떤 문제를 풀었는지**가 이식할 내용이다.

### 자산 밴드: 이미지 / 비디오 / 오디오를 한 줄에

- 세 열이 한 가로 밴드를 공유한다. 세로로 쌓으면 이 블록이 패널의 1/3을 먹고, 그 높이는
  전부 클립 에디터에서 빠진다
- **이미지 열은 9칸 고정 폭**(534px = 9×54 + 8×6). 붙어 있는 이미지 수에 맞춰 줄이면
  사진을 넣고 뺄 때마다 오른쪽 비디오·오디오 열이 좌우로 흔들린다
- 이미지 그리드는 `flexWrap: nowrap` — 줄바꿈되면 옆 열이 같이 밀린다
- 모드 버튼(`First/Last` / `Reference`)을 이미지 열 **안으로** 넣고, 각 미디어 열의 캡션과
  같은 헤더 높이(24px)를 준다. 이게 세 종류 썸네일을 같은 줄에 세우는 방법이다
- `Brief: ... / Vision: ...` 모델 줄은 이미지 열 맨 아래에 `marginTop: auto`로 붙인다.
  밴드 아래 별도 행으로 두면 패널에 없는 높이를 더해 하단 버튼이 잘린다

### 헤더 / 사운드-뮤직 상자

- 플렉스 행에 눌려 `min-height: 90px`를 못 지키고 55px로 찌그러졌다 →
  `flexShrink: 0` + 명시적 높이
- 내용에 맞춰 자동으로 늘어난다(104~320px). 긴 헤더가 기본이라 4줄씩 스크롤해 읽는 상황이
  정상이 아니다

### LOCAL ENHANCE 아코디언

`▾` / `▸` 토글. 접으면 이 블록이 바닥의 바 하나로 접히고 **그 높이가 전부 에디터로 간다**
(노드 실측: 에디터 230 → 512px). 접힘 상태는 `state.enhCollapsed`로 저장한다.

주의: 이미지 행 자체의 표시/숨김(`enhMode === "image"`) 로직이 접힘 상태를 되살리지 않도록
할 것. 노드에서는 원래 display 값을 따로 들고 접힘일 때는 무조건 숨긴다.

### Enhance 바는 한 줄

```
→ this clip ▾  |  Length  3:20  |  → 2 clips (from the editor)  |  ✨ Enhance
```

공용 `select()` 헬퍼가 `width: 100%`라 드롭다운 혼자 한 줄을 다 먹고 있었다.
`width: auto` + 행 `nowrap`, 가운데 안내문이 남는 공간을 흡수해 Enhance가 오른쪽 끝에 붙는다.

---

## 11. 레퍼런스 비디오 / 오디오 슬롯 (전면 교체)

기존은 항목마다 세로로 긴 폼(드롭다운 + 업로드 + in/out + 정보 + 체크박스)이었다.
미리듣기·미리보기가 아예 없고, 3개를 놓으면 이미지 그리드 전체보다 높았다.

**이미지 타일과 같은 형태로 바꾼다.** 노드에서는 `ui_clip_media_slots.js` 한 모듈이
Prompt Edit(클립 전용)과 **왼쪽 패널(공통)** 양쪽에 쓰인다.

### 타일

- **72 × 54 직사각형** (이미지 타일은 54×54 정사각). 16:9 영상을 정사각으로 자르면 양옆이
  날아가고, 오디오는 파일명을 보여줄 가로 폭이 필요하다
- 번호 + `✕` + 클릭 시 업로드
- **비디오**: 프레임을 보여주고 **마우스 오버 시 무음 재생**. `🖼` 버튼으로 미니맥스
  갤러리에서 선택
- **오디오**: 썸네일 대신 **파일명**(자동 줄바꿈, 호버 시 툴팁으로 전체 이름)
- 채워지지 않은 칸도 항상 3개를 그린다 — 옆 블록이 흔들리지 않게

### 타일 아래 (전부 썸네일 기준 가운데 정렬, 최소 글자 크기 9.5px)

```
▶  ⏮          ← 재생/정지 토글 + 처음부터
0:00 / 0:59    ← 구간 내 경과 / 구간 길이
▬▬▬●▬▬▬        ← 오디오만. 핸들 6px
in  [ 0  ]
out [59.4]
59.40s
30fps→24
has audio
```

- 재생 버튼은 **재생/정지 스위치**, `⏮`은 처음부터
- 소스 정보는 **한 줄에 하나씩.** `·`로 이으면 72px 폭에서 아무 데서나 끊긴다
- 슬라이더 핸들은 인라인 스타일로 못 줄인다 — `::-webkit-slider-thumb` /
  `::-moz-range-thumb`가 필요하므로 스타일시트를 한 번만 주입한다

### in / out은 이 타일이 재생하는 구간이다

단순히 렌더에 넘기는 숫자가 아니다. 구간을 고르는 게 목적이므로 **재생·정지·⏮·스크럽 바·
시계가 전부 `[in, out]` 안에서만** 움직이고, out에 닿으면 자동으로 멈춘다. 마우스 오버
미리보기도 in부터 재생한다. 시계는 파일 전체가 아니라 **구간 내 경과 / 구간 길이**를 쓴다.

in/out을 고치면 **즉시** 반영된다 — 재생 위치가 구간 안으로 이동하고 시계가 다시 그려진다.
전체 재렌더 없이 처리한다.

### 파일을 처음 넣으면 in = 0, out = 전체 길이

길이는 메타데이터가 와야 알 수 있으므로, 넣는 순간에는 out을 `0`(= 아직 모름)으로 두고
`media_info` 응답이 오면 채운다. **out이 한 번도 설정된 적 없을 때만** 채워서, 사용자가
조정한 구간은 나중에 덮어쓰지 않는다.

기존의 `end: 5` 하드코딩은 없앴다 — 어디서 온 값인지 알 수 없는 창을 사용자가 눈치채고
고쳐야 했다.

### out은 파일 길이를 넘을 수 없다

없는 프레임을 요구하면 렌더가 실패하는데, 숫자만 봐서는 그게 원인인지 알 수 없다. 세 겹으로 막는다.

1. **입력 시 클램프** — 저장값만이 아니라 **입력칸 표시도 같이 고쳐 쓴다.** 저장만 고치고
   화면에 9999가 남아 있으면 입력이 받아들여진 것처럼 보인다
2. **`max` 속성** — 스피너 화살표 경로 차단
3. **로드 시 교정** — 다른 파일 기준으로 저장됐거나 길이를 알기 전에 입력된 값을 줄인다

`in`은 뒤에 최소 0.1초를 남기도록 `길이 - 0.1`이 상한이다.

---

## 12. 미니맥스 갤러리에서 비디오 고르기

레퍼런스 비디오는 대개 이 노드가 방금 만든 클립이다. 디스크에서 파일을 찾아 다시 업로드할
이유가 없다.

- `GET /minimax_h3_one/videos`로 출력 폴더의 클립을 읽는다
- 호버하면 무음 재생 — 비슷한 테이크가 벽처럼 늘어서면 첫 프레임만으로는 구분이 안 된다
- 고르면 `POST /minimax_h3_one/copy_to_input`으로 `input/`에 복사하고 그 이름을 슬롯에 넣는다.
  로더 노드가 읽을 수 있는 곳이 거기뿐이다

### 함정: `aspect-ratio`로 썸네일 높이를 잡지 말 것

메타데이터가 로드되기 전 `<video>`는 고유 크기가 없어서 비율이 아무 값으로도 풀리지 않는다.
셀이 21px로 무너졌고, 그 결과 120개가 한 화면에 다 들어가 **스크롤이 아예 생기지 않았다.**
고정 높이를 줄 것.

다이얼로그는 고정 높이 플렉스 컬럼으로 만들고, 그리드에 `flex: 1 1 auto` + **`minHeight: 0`**
+ `overflowY: auto`를 준다. `minHeight: 0`이 없으면 플렉스 자식은 줄어들지 못하고 다이얼로그가
같이 커진다.

---

## 13. 그 외 부수 변경

- **노드 폭 +25%** (1000 → 1250px). 오른쪽 패널이 `flex: 1`이라 늘어난 폭을 프리뷰가 흡수하고
  왼쪽 320px 메뉴는 그대로다. 웹은 레이아웃이 다르니 그대로 옮길 필요는 없다
- **갤러리 이미지 피커에 INPUT 폴더 탭 추가, 기본 선택.** Prompt Edit 첨부 칸에도 갤러리
  선택 버튼(🖼)이 붙는다
- **디블러는 업스케일과 독립된 단계**다. 각각 자기 `None`을 갖고, 업스케일 없이 단독 실행된다.
  해상도를 바꾸지 않는다(정의상 동일 해상도)
- **노드 전체화면** — 브라우저 전체화면이 아니라 노드 DOM을 body 직속으로 옮겨 배율 확대.
  `position: fixed`는 변형된 조상 기준으로 계산되므로 캔버스 컨테이너 안에 두면 절대 안 된다.
  종료는 **토글 버튼으로만** — Esc나 여백 클릭은 작업 중 실수로 터진다

---

## 14. 사용자 프리셋 저장/관리 (파이프라인 프리셋)

`SPEC_MINIMAX_H3_PRESETS.md`는 **내장 프리셋 6개**만 다룬다. 사용자가 자기 조합을 저장하는
쪽은 그 문서에 없으므로 여기 적는다. §7의 **프롬포트 세트**와는 다른 기능이다 — 이쪽은 왼쪽
패널의 파이프라인 설정(터보·어텐션·캐시·Spectrum·모델 패치 등)이다.

### 동작

드롭다운 아래 **Save** / **Setting** 두 버튼. 드롭다운과 같은 전체 폭을 **50:50으로 나눠
가진다.** 드롭다운과의 세로 간격은 5px.

- **Save** — 현재 파이프라인 축을 이름 붙여 저장. 같은 이름이 있으면 덮어쓸지 묻는다
  (조용히 동명 항목을 하나 더 만들면 드롭다운에서 구분이 안 된다)
- **Setting** — 저장된 목록을 드래그로 순서 변경 · 이름 변경 · 삭제. 삭제는 되돌릴 수 없는
  유일한 동작이라 항상 확인한다

### 드롭다운 구성

```
— Custom (current settings) —
────── User Preset ──────      ← disabled
★ 사용자 프리셋 1
★ 사용자 프리셋 2
───── System Preset ─────      ← disabled
18. [Everyday] ...
```

- 사용자 프리셋이 **내장 프리셋 위**에 온다
- 두 구분자는 `disabled: true` — 선택되면 안 된다
- **내장 6개는 읽기 전용.** 사용자 프리셋이 내장 것을 가리면 공유된 프리셋 번호가 뜻하는
  바가 조용히 달라진다

### 저장 위치

브라우저가 아니라 **서버**에 둔다(노드는 config의 `user_presets`). 브라우저를 초기화해도
남아야 하고, 무엇보다 프리셋은 남에게 번호로 알려주는 물건이다.

### 함정

Rename / Delete가 `window.prompt` / `confirm`을 쓰다가 죽어 있었다 — §9 참고. 이 기능에서
처음 발견했고, 나중에 프롬포트 세트에서 같은 원인으로 또 겪었다.

---

## 이식 순서

이 순서를 지킬 것. 4번이 1번보다 먼저다.

1. **Reuse 이미지 복원** (§4) — 안 하면 복원 불가 세트가 N배로 늘어난다
2. **Ollama 제거** (§6) — LLM이 하나로 줄어든 뒤에 오버라이드를 해야 한다
3. **클립별 오버라이드** (§1) + **헤더/테일 오버라이드** (§2) — 같이 간다
4. **프리셋 자산 저장/로딩** (§7) + **고스트 썸네일** (§8) — 같이 간다.
   §7만 하면 없어진 파일이 조용히 사라진다
5. **미디어 슬롯 교체** (§11) + **비디오 피커** (§12) — 같이 간다
6. 나머지 (§3, §5, §9, §10, §13, §14)

---

## 검증 방법

**문법 검사만으로 완료 선언하지 말 것.** 노드 쪽에서 그렇게 했다가 사용자가 버그를 하나씩
찾아내는 일이 있었다. 브라우저에 실제로 띄우고, 클립 두 개짜리 상태로 오버라이드를 켜고 끄며
확인한다. 렌더 검증은 0.1MP · 최소 클립 길이 · 낮은 스텝이면 충분하다.

**왕복은 양쪽을 다 확인할 것.** 프리셋에서 Load만 확인하고 "검증 완료"라고 한 적이 있는데,
그때 Save 버튼은 죽어 있었고(§9) 서버는 자산 필드를 버리고 있었다(§7). 저장이 안 되면 그
기능은 쓸 수 없다. 지우고 → 불러와서 → 돌아왔는지까지 봐야 왕복 검증이다.

**상태는 UI로 넣을 것.** 노드에서 위젯의 JSON 문자열을 직접 고쳐 테스트하다 몇 번 헛돌았다.
오버레이는 라이브 state 객체를 클로저로 잡고 있어서 직렬화된 위젯 값을 고쳐도 반영되지 않는다.
실제 입력칸에 타이핑하고 실제 버튼을 눌러야 그 경로를 검증한 것이 된다.

**고친 기능 안에서 고장을 발견하면 그 자리에서 고칠 것.** 범위를 이유로 남겨두고 완료
보고하지 말 것.
