# PORT LEDGER — node ↔ web twin

One row per portable change between `ComfyUI-TJ_NODE_STUDIO_ONE` (the node) and
`AI_One_Studio` (the web twin). Both sessions write to this file. It exists so
node/web drift stops depending on one session's memory of what it already relayed
— three things went unported on 2026-08-31 and every one was a *relay* failure,
not a coding failure.

## How to use it

- Add a row the moment a change lands on either side, even if the other side is
  still `—`. A missing row is the failure mode this file prevents.
- `origin` records **which way the change travelled**:
  - `node→web` — spec-driven: built and verified on the node, relayed via a `SPEC_*.md`
  - `web→node` — found on the web side and reported back upstream (bugs in shipped
    node code have gone this way)
  - `user` — the user asked for it directly on one side; never entered a spec
- `verified` is the date the change was confirmed **working in a browser** on the
  side that lists it — not the commit date, not a syntax check.
- The spec carries **why** and the traps. The web reads the **node source** for
  what to build. That switch is what made the web ports start landing correctly.

## Anchors

| repo | commit | meaning |
|---|---|---|
| node | `da412e7` | v1.20.0 — the whole §1–§16 bundle |
| node | `cc644fe` | chunked-output suffix fix + dead `ollamaImages` list removal |
| node | `b31afc6` | README brought up to v1.20.0 |
| web  | `91ccc3b` | `ollamaImageMode` → `briefImageMode` rename |
| web  | `8d17c6b` | §6 note updated after node `cc644fe` |

## SPEC_MINIMAX_H3_PER_CLIP_OVERRIDE.md

| § | item | node | web | verified | origin | notes |
|---|---|---|---|---|---|---|
| 1 | Per-clip reference override (explicit checkbox, all-or-nothing, `clipAssets` resolver) | `da412e7` | `9a023ce` | 2026-08-31 | node→web | web `355da29` fixed a real port bug: the override was bound to the vision-only image list, so ticking the box changed nothing in the render. Bind to `refImages`. |
| 2 | Header + sound/music tail follow the override (`clipFraming`, `refreshFraming` at 3 call sites) | `da412e7` | `9a023ce` | 2026-08-31 | node→web | Missing from the node's own first implementation; must ship with §1. |
| 3 | Prompt Edit opens in the enhance mode that matches the node mode | `da412e7` | `9a023ce` | 2026-08-31 | node→web | Trap: text-only state value is `"t2v"`, not `"text"`. Confirm the web's value. |
| 4 | Reuse restores images (`metaForVideo` writes ref fields, `reuseAll` puts them back) | `da412e7` | `9a023ce` | 2026-08-31 | node→web | Must precede §1 — otherwise unrecoverable image sets go from 1 to N. |
| 5 | Upscale / deblur / stitch output metadata (`composeStitchedPrompt`, `[Clip N]` dividers, split on Reuse) | `da412e7` | `9a023ce` | 2026-08-31 | node→web | |
| 6 | Ollama removed, native vision only (`visionSource` default flipped, routes dropped, `LOCAL ENHANCE (native CLIP)`) | `da412e7`, `cc644fe` | `9a023ce`, `91ccc3b`, `8d17c6b` | 2026-08-31 | node→web | `cc644fe` removed write-only `ollamaImages` + `syncImageLists()`; renamed live `ollamaImageMode` → `briefImageMode` (migration reads old key). Web matched in `91ccc3b`. 5 dead `ollama_*` keys still sit in the node's config file; web dropped them on migration. |
| 7 | Prompt sets save/load images + video + audio (payload `v: 2`, whole `prompts[]` entry, old-set protection) | `da412e7` | `292fc54` | 2026-08-31 | node→web | Trap: the server save route whitelist silently dropped every field except `promptHeader`/`promptFooter`/`prompts`. Client-only fix leaves assets missing with no error. |
| 8 | Ghost thumbnails for missing attachments (`POST .../input_exists`, batch lookup, `state.missingAssets`) | `da412e7` | `292fc54` | 2026-08-31 | node→web | Names come from saved state — never trust as a path; `basename(n) != n` → treat as missing. |
| 9 | No browser `prompt`/`confirm` (ComfyUI suppresses them); shared `ui_ask.js` | `da412e7` | `7a417f8` + `7fd17d9` | 2026-08-31 | node→web | Web `7a417f8` = `confirmDialog()`/`promptDialog()` overlays in `src/shared/ui.ts`, all `confirm`/`prompt` call sites swapped. Node's `ui_ask.js` covers **`kind: "text" \| "confirm"` only** — no alert. Info/error popups on the node go through a separate transient toast: `showPopup(msg, isError)` in `one_node_minimax_h3.js` (auto-fades 4s, `.mmh3-pop`), and `ctx.showPopup` is threaded to every submodule. Web straggler found 2026-08-31: `src/tools/minimax_h3/view.ts confirmStopIsOurs()` still called `window.confirm` → silently `false` in the preview browser, Stop button dead while a foreign job ran; fixed to `confirmDialog()` in web `7fd17d9`, which also added `alertDialog()` to `src/shared/ui.ts` for the one gallery-card info-button `alert()` (multi-line meta needs a real OK, not an auto-fade toast). Web MiniMax H3's `showPopup` toast was already fully threaded (~45 sites) and matches the node. Other web tools' error `alert()`s (anima/klein/krea2/qwen/sdxl/zimage) left as-is — matches the node not sweeping its other tools. |
| 10 | Prompt Edit layout — asset band (image/video/audio one row), header/tail boxes, LOCAL ENHANCE accordion, one-line Enhance bar | `da412e7` | `a1c8f1b` | 2026-08-31 | node→web | Web layout differs — port *which problem was solved*, not the pixel dimensions. `a1c8f1b` did the functional part (collapsible LOCAL ENHANCE block). |
| 11 | Reference video/audio slots rebuilt as tiles (`ui_clip_media_slots.js`, 72×54, hover preview, in/out = playback window, clamp to file length) | `da412e7` | `566db78` | 2026-08-31 | node→web | Same module serves Prompt Edit (per-clip) and the left panel (common). Killed the `end: 5` hardcode. |
| 12 | Pick reference video from the MiniMax gallery (`/videos`, hover play, `copy_to_input`) | `da412e7` | `566db78` | 2026-08-31 | node→web | Trap: no `aspect-ratio` for thumb height before metadata loads — cell collapses, scroll never appears. Fixed height + `minHeight: 0` on the flex grid. |
| 13 | Misc — node width +25%, INPUT-folder picker tab, deblur independent of upscale, node fullscreen via DOM reparent | `da412e7` | partial | 2026-08-31 | node→web | Width/fullscreen are node-shell concerns; INPUT tab + independent deblur are the portable parts. |
| 14 | User pipeline presets (Save / Setting, server-stored `user_presets`, user presets above the 6 read-only builtins) | `da412e7` | `1b083f0` | 2026-08-31 | node→web | Distinct from `SPEC_MINIMAX_H3_PRESETS.md` (that doc = builtins only). Trap: Rename/Delete died on `window.prompt`/`confirm` — see §9. |
| 15 | RTX Deblur — pre-upscale stage with its own `None`, same resolution in/out, own node `TJ_RTXDeblur` (not a patch to the RTX pack), hard failure if SDK absent | `da412e7` | `1a461cf` | 2026-08-31 | node→web | `VideoSuperRes` quality levels 12–15; do not reuse the upscale's scale math or size changes silently. `_deblur` suffix, copies source metadata (§5). |
| 16 | Gallery post-process runs long video in chunks (byte-budget sizing, no threshold, stream-copy concat, cleanup on both paths) | `da412e7`, `cc644fe` | `64e1c9b` | 2026-08-31 | node→web | **Relay gap** — landed in v1.20.0, was in no spec; the web caught it only by reading the node source directly. `cc644fe` then fixed the node: chunked output joined under the source's bare stem, colliding with fresh renders — `runPost` now applies the caller's suffix. |

## Outside the spec

| item | node | web | verified | origin | notes |
|---|---|---|---|---|---|
| Node v1.24.1 security pass — removed `POST /tj_studio_one/llm/install_tj_node` (server-side `git clone` + `pip install` over HTTP) and added an SSRF guard to `POST /tj_studio_one/llm/download_image` (http(s) only, host must not resolve to loopback/private/link-local/reserved, no redirects, 64 MB cap). | `v1.24.1` (`da3e261`) | n/a | 2026-09-03 | node | **Web has no server-side surface to mirror.** `AI_One_Studio` is a pure static Vite SPA — no Python, no Node server, no `server/`/`api/`/`functions/`, no serverless/edge config, no vite proxy; `package.json` scripts are just `vite` / `tsc && vite build` / `vite preview`. Every network call in `src/` runs in the end-user's browser. The one URL-fetch feature — "Download from URL" in the LLM prompt tools (`src/tools/*/promptTools.ts` ×6: anima/klein/krea2/qwen2511/sdxl/zimage) — POSTs `{ url }` to the node's `/tj_studio_one/llm/download_image` route, i.e. the exact route the node just guarded. `comfyBase.ts` `getComfyBase()` only ever returns `http://127.0.0.1:<port>` (local) or the build-time `VITE_COMFY_URL` constant — no user input, no proxy. Confirmed 2026-09-03. |
| Move Sampler / Scheduler / Denoise / Sigma-Shift into a left-panel Sampling accordion | `075b9d1` | `d4d4fe6` | 2026-08-31 | user | Asked for on the web side, never relayed to the node in either direction. Node still has these where they were — **open port, web→node**, if the node should match. |
| Panel keeps the settings you last used, not a workflow's older serialised copy (`preferNewerThan`, queue-time stamping) | `3cdca12` | `075b9d1` | 2026-08-31 | node | Node-only so far. Check whether the web twin has the same stale-restore path. |
| **H3 Optimizer axis** (`SPEC_MINIMAX_H3_PIPELINE_AXES.md` §③) — a third Attention-accordion control wrapping Zironic `H3-Optimizations`. `h3Optimizer`: `none` / `memory` (`H3MemoryOptimization` — wraps the *selected* dense backend incl. Comfy Kitchen; never blocked) / `memory_sparse` (`+ H3SparseAttention`). User: "옵티마이저를 넣어줘 지원 가능한 어텐션에서는 다 활성화 되게". | `v1.24.0` (`4fcabb3` → `b34876c`, tag `v1.24.0` + Registry publish) | `25d6848` → `b61dace` → `87b26ac` (release `0.2.0`, tag `v0.2.0`) | 2026-09-03 | user → both | Node authoritative, web mirrors. `h3OptimizerBlockedReason`: `memory` never blocked (its MLP/FinalLayer/embed savings patch `blocks[i].forward`, a different key from attention); `memory_sparse` gated like the sparse backends — out under any turbo, refused on `solattn_kijai`/`sla`, and (`b34876c`, traced through `h3_optimizations/patch.py`) refused when an **H3 forward patch** is on: H3 Sparse and the forward patch both claim `blocks[i].attn.forward` and H3-Optimizations skips a foreign patch → Sparse goes inert. Auto-falls to `memory` (not `none`). `h3OptimizerOverlapNote` now fires **only for `memory`** with accurate text — the forward patch keeps the attention, the optimizer's MLP/FinalLayer/embed savings still stack. Aside in §③: Fused Modulation also patches `blocks[i].forward` → disables Memory Opt's MLP chunking for those blocks (non-fatal). graphBuilder `N.h3mem`/`N.h3sparse` after the block caches, inside Spectrum's wrapper; `H3MemoryOptimization` gets the legacy hidden inputs so the API graph validates. view.ts `h3OptimizerSettings()` (select + precision/qkv/Lower-VRAM + budget/denser-edges + note), summary `· MemOpt[+Sparse]`, `fixupPipelineAxes` reset, clip-meta `h3Optimizer` (+`h3SparseBudget`) + `applyClipSettings` restore. `api.ts` `MMH3_OPTIONAL_NODES` += both nodes. `install_comfyui_dependencies.bat` REPOS[22] `Zironic/H3-Optimizations` (ALT `h3-optimizations`, no pip). Browser-verified vs live ComfyUI (pack was installed): gating, overlap note, graph chain `H3MemoryOptimization → H3SparseAttention`, all UI. `SPEC_MINIMAX_H3_OPTIMIZER_AXIS.md` folded into pipeline-axes §③ rather than a new file. |
| **H3 attention forward patch no longer gated on the backend.** `SPEC_MINIMAX_H3_PIPELINE_AXES.md` (2026-08-26) said to *block* MemEff Sage / SolAttn-Saganaki whenever `attnBackend` is `ck` / `solattn_kijai` / `sla`. Node `0876abc` (2026-08-27) reversed it — the forward patch replaces `blocks[i].attn.forward` and it's the faster half; the override backend is the one that stops reaching the transformer blocks. Spec predated the reversal so the web ported the wrong model: web greyed the forward dropdown + dropped the graph node under CK. User hit it trying CK + MemEff Sage. | `0876abc` (2026-08-27) | `a09d33f` | 2026-09-03 | user → web | Web `attnForwardBlockedReason` now blocks only `larryvrh` + a non-dense (sparse) forward kernel (`ATTN_FORWARDS` gained `dense` flags); new `attnForwardOverlapNote(state, key)` renders a hint under the H3-forward select instead of greying it. `graphBuilder` guard unchanged (only trips on larryvrh+sparse now = node parity). `migratePipelineState`: SLA no longer claims the backend slot ahead of CK/Sage (only if nothing else did). **Divergence**: web keeps its `== null` migration guard and does *not* force a re-migration (node's `PIPELINE_MIGRATION = 2`) — the node's re-run relies on the old `use*Attention` booleans, which the web still round-trips through server config, so a forced re-derive there could clobber a state the user set through the new UI. Browser-verified: `ck+memeff` / `sla+memeff` / `ck+saganaki` selectable with the note; `larryvrh+saganaki` still blocked; graph chain `ModelAttentionBackend → MiniMaxH3MemoryEfficientSageAttentionPatch`. `SPEC_MINIMAX_H3_PIPELINE_AXES.md` §2 rewritten. |
| Extend forcing `firstlast` doesn't carry the PDD accelerator / Turbo LoRA when the source clip used them in **reference** mode — `pddFileForMode` reads `pddFile` not `pddFileReference`. | `v1.21.1` | `ea4f541` | 2026-09-01 | web→node | Found during the v1.21.0 web verify (§3 real render). User pulled the "do it in the morning" call — finished same night. `runExtend` now copies `pddFileReference`→`pddFile` and `turboLoraReference`→`turboLora` when the primary slot is empty, right after forcing the mode. Web skips the SLA turbo LoRA line (no dedicated state field — plain LoRA-list entry). No live render (both sessions agreed the 2-line guard + tsc is enough); node side verified. |
| Settings → Models "PDD Acc file · FL2VA / Ref2VA" pickers didn't persist — reopen Settings and both were back to `none`. Web: `saveConfig` payload + `getConfig` take-block never carried `pdd_file` / `pdd_file_reference` (only localStorage held them). Node: `mmh3_save_config` writes them via `cfg.update(patch)` but `mmh3_get_config` (~line 1899) omits them from its response dict, so `take("pddFile", cfg.pdd_file)` always got undefined — node's own persistence silently broken the same way. | `v1.22.3` (`c7f8921`) | `2b73dbf` | 2026-09-01 | user → web, web→node | Web fix mirrors `turbo_lora`: `pdd_file` / `pdd_file_reference` added to `MmhConfig`, `saveAll()` payload, `getConfig().then` take-block. Node v1.22.3 adds `pdd_file` / `pdd_file_reference` / `pdd_nfe` / `pdd_lora_strength` / `pdd_head_strength` to the `mmh3_get_config` response dict — end-to-end round-trip now works both sides. Web keeps `pdd_nfe` / `pdd_lora_strength` / `pdd_head_strength` / `turbo_lora_reference` on the per-run left-panel path (localStorage), not server config — no Settings UI field for them. |
| "Sampling · step N/M" showed wrong totals (30-step run flickering to `38/60`, `15/36`, turbo `7/8`). The progress handler forwarded every ComfyUI `progress` WS event with no `node` / `prompt_id` filter, so `ModelPreviewOverrideKJ` (per-step live preview) and the video `VAEDecode` (~60 latent frames) bled into the sampler readout. Not a reconnect-only issue — every run. | `v1.22.4` (`f50cce7`) | `eafe2c3` | 2026-09-01 | user → web, web→node | Both sides: `queuePrompt` (+ node's `waitForHistory` resume path) gains optional `samplerNode`; the progress handler drops events whose `node` ≠ `NODE_IDS.sampler` (`"MM:sampler"`, the `SamplerCustomAdvanced` — a constant, valid on resume too) or whose `prompt_id` ≠ the running job. Unset on graphless reconnect + gallery post-process (forward-all is right there — tracks the upscale/chunk bar). Forward-all fallback when the event has no `node` (older ComfyUI). |
| `install_requirements.bat/.sh` used the wrong Python on ComfyUI Desktop — it only checked `<ComfyUI>\python_embeded` / `<ComfyUI>\..\python_embeded` / a couple `venv` spots, then fell through **silently** to `where python` (system Python). ComfyUI Desktop runs from `<ComfyUI>\.venv` (a uv venv; base = `<base>\standalone-env`, ~676 pkgs incl. torch; the bare `standalone-env` has ~198), so deps went to system Python and the app never saw them. | `v1.23.3` (`3008040`) → `v1.23.4` (`e873f83`) → `v1.23.5` (`d8020ae`) | `270fcc4` → `ce7a153` → `77ad00e` (`install_comfyui_dependencies.bat`, already has `PYTHONUTF8=1`); `1d4eea7` (CRLF) | 2026-09-03 | user → both | Same fix both sides: optional first arg = the ComfyUI folder (verified via `main.py`); **7-entry discovery order** `<ComfyUI>\venv`, `<ComfyUI>\.venv`, `<base>\venv`, `<base>\.venv`, `<base>\standalone-env`, `<ComfyUI>\python_embeded`, `<base>\python_embeded` (a real venv wins over the base interpreter — installing into bare `standalone-env` = same as system Python). System Python is **opt-in only** (names it, asks Y/N, else skips all pip — repos still clone). Node uses a `for %%P in (...)` list; web uses a `:TryPy` subroutine — same order, same outcome. **`v1.23.4` / web `77ad00e`**: the `for /f ('"%PYTHON%" -m pip show numpy ^| findstr /c:"Version:"')` numpy probe printed `The filename ... is incorrect` (cmd /c strips the outer quote pair off a command that starts with a quote and holds more) → numpy pin-back silently skipped; both replaced it with a `:GetNumpyVer` / `:GET_NUMPY_VER` sub (pip → temp file → `for /f` over `findstr ... "<file>"`). **Web `1d4eea7`** (web-only): both `.bat` were LF — cmd.exe misparses LF-only .bat once labels/subroutines are involved; re-saved CRLF + ASCII, `.gitattributes` marks `*.bat`/`*.cmd`/`*.ps1` `-text`. Node repo already had `*.bat text eol=crlf`. Both verified on this PC: portable → `python_embeded` (3.12), Desktop → `ComfyUI\.venv` (3.13, matches the app's `Python executable:` log line), numpy probe returns 2.4.4 with no error, full run to "Done" exit 0. **`v1.23.5`** adds `set "PYTHONUTF8=1"` at the top (web `install_comfyui_dependencies.bat` already had it): on cp949 Windows `groundingdino-py`'s `setup.py` (via `ComfyUI-RMBG`) crashed with `UnicodeDecodeError` so RMBG's reqs never installed — forcing UTF-8 text I/O for the run fixes it, confirmed the setup.py runs clean with the flag set. Node ran a **full end-to-end `install_requirements.bat "C:\AI\Comfy-Dasktop\ComfyUI"`** 2026-09-03: `.venv` python 3.13, no quote-mangle error, 22/22 packs handled, exit 0 (the only WARN is `ComfyUI-TJ_NODE`'s local edits blocking its ff-only pull — expected). |
| Installer was missing **`ComfyUI-VFI`** — `RIFEInterpolation`, which the gallery's "Interpolate a finished clip" post-process needs. Not in either script's `REPOS` nor `dependency_check.py`, though `MMH3_OPTIONAL_NODES` already checked for the node → a fresh install always reported interpolation missing. | `v1.23.6` (`8f662b8`) | `579ddf9` | 2026-09-03 | user → both | Repo is **`GACLove/ComfyUI-VFI`** (the older `ModelTC/ComfyUI-VFI` 404s — stale pyproject Repository field). Cloned as folder `rife_comfyui_wrapper` (ComfyUI Manager's pyproject-`name` for it) so a later Manager install/update doesn't drop a duplicate + collide the node class. Web: `REPOS[21]` + a minimal `ALT[21]` folder override (`if defined ALT[%IDX%] set FOLDER=...`), `COUNT`/loop bumped to 21, dry-run to "Done" exit 0. Web-side availability check needs nothing — `refreshInterpAvailability` reads `ctx.availability.RIFEInterpolation` straight from the node's `/node_availability`. (Separate finding same session: the "missing" Sol Attention patches — `MiniMaxH3ScheduledSolAttentionPatch` — are already in both installers; they just need the Triton runtime that ships with SageAttention. Not an installer bug.) |
| Missing-dependency-pack warnings (v1.20.4: post-copy popup removed as a ClickFix false-positive — Win32/ClickFix quarantine, plausible Registry-flag cause): startup console banner (`dependency_check.py`), then a persistent red/amber strip on the MiniMax H3 node with the installer's absolute path, per-line Copy buttons and a step-by-step run-it popup; `/minimax_h3_one/node_availability` returns `install_dir` + script names; `install_requirements.*` now also clone `ComfyUI-TJ_NODE` and restore numpy | `291d84a` (v1.20.1) → `v1.20.3` | n/a | 2026-08-31 | node → web | Console check reads only `custom_nodes/` dir names; no subprocess/network. **Relayed + decided 2026-08-31**: (a) numpy record/restore — web done (`AI-ONE-STUDIO` `05a7044`); web installer already clones `ComfyUI-TJ_NODE` (REPOS[19]). (b) **NO auto-run installer route** — studio.tjtj.cloud is cloudflared-public and an exec route on ComfyUI would be an internet-reachable RCE, matching the pattern under Registry-flag investigation. Web mirrors the node's guidance instead: script path + Copy button + "open cmd, paste, run, restart" in the missing-node warnings. Supported web setup = manual 5-step (install ComfyUI → clone kit → run install script → start server → open app). Web guidance-text port pending. |
| Gallery "no meta" — first report (K2_00108_.png etc. on iPhone Safari) was a per-device Cloudflare Access login gap, not code (see below). **Second, distinct report** (reproduced on PC + phone both logged in) was real: Krea2/Z-Image/Anima/Qwen2511/Klein/SDXL's gallery "♻ Reuse" always showed "No meta" even though the backend had it. `loadMeta()` in each tool's `api.ts` returned the raw `{ok, meta}` response, but `galleryOverlay.ts`'s Reuse handler checked `meta.mode` on that wrapper (`mode` lives at `meta.meta.mode`) — so the check was always false, for every clip, in all 6 tools. MiniMax H3 was unaffected (it reuses the `meta` already embedded in the gallery list response, never a separate `/meta` fetch). | n/a | `<pending>` | 2026-09-05 | user | Fixed by unwrapping in `loadMeta()` itself (`return d.ok ? d.meta : null`) rather than each call site — one-line change × 6 files (`anima/klein/krea2/qwen2511/sdxl/zimage/api.ts`). Browser-verified against the live local ComfyUI: Krea2 gallery Reuse on a real clip now restores steps/cfg/sampler/scheduler/seed/prompt instead of showing "No meta". `tsc --noEmit` — no new errors. |
| Gallery "no meta" / "reuse broken" reports after Cloudflare Access hardening on `comfy.tjtj.cloud` — **not a code or credentials bug**. `credentials:"include"` was already present on every web fetch (this session verified). Root cause traced to a **per-device Access login**: the affected device was iPhone Safari, which has its own cookie jar separate from desktop Chrome and had never completed the Access login page for that domain. Logging in once in Safari fixed it; desktop Chrome had already tested `/minimax_h3_one/meta` directly (200, `ok:true`, fields present). | n/a | n/a | 2026-09-05 | user | If this recurs, check whether the device hitting it has ever completed the Cloudflare Access login prompt for `comfy.tjtj.cloud` on its own browser — don't assume a code regression. |
| **"⬇ Agent JSON" download** — h3/krea2/zimage/music each got a UI button that exports the panel's current settings as the matching `*-headless`'s `job` object, wrapped `{"tool","job","target":""}` (envelope confirmed against real Hermes job files the user shared; `target` left blank, web has no notion of it). h3 is per-clip (`job_<seed>_C<n>.json`, mirrors `clipAssets`/`promptFirstFrame` resolution exactly); the others are one button per tool (`<tool>_<mode>.json`). Image fields (h3 `refImages`/`firstFrame`/`lastFrame`, krea2 `i2iImage`/`identityImage(B)`, zimage `i2iImage`) are rewritten to `/Users/hermes/.hermes/render-queue/inputs/<filename>` — the user's new Hermes local layout (`inputs/`→`outputs/`→`archive/{images,videos,music}/`), not this studio's own filename. | n/a | `a986457` → `d23944e` → `46409d8` → `796f9ef` → `272897a` → `a4582fe` | 2026-09-08 | user | music has no real Hermes sample yet — built from `music-headless/README.md`'s schema, needs a real-sample check later. Node confirmed tilde expansion is also fine (`93b0eb1` added it to krea2/music-headless `abspath()`); web kept the absolute-path export either way (works regardless, more portable in the job file). Same 3-line tilde-expansion patch (`homedir()`-based, matching krea2/music-headless) applied to h3/zimage/klein/upscale/video-rtx-headless's `abspath()` for parity, same commit range as this row. Envelope (`tool`/`target`) and job-filename conventions are **Hermes queue-worker concerns, not headless-CLI or node/web spec** — confirm with the Hermes bot side, not this ledger. |
| **`hermes-job-json` Claude Code skill** — `AI_One_Studio/.claude/skills/hermes-job-json/` (`SKILL.md` + `write_jobs.mjs` 0-dep Node20+ + `reference/schemas.md` + `reference/envelope.md`). Complement to the per-panel "⬇ Agent JSON" button: turns a bulk request ("krea 자유주제 이미지 200장", "T2VA 영상 200개", "10초×10편 스토리") into **N** `<tool>_<mode>_NNN.json` files (`{tool,job,target}` envelope) + `manifest.json`. Collects tool/mode/count/batch·sequence/theme; delegates H3 prompt authoring to the `minimax-h3-prompt` skill; writes the fl2va relay note (clip1 t2va, clip2-N fl2va `firstFrame:null` + `manifest._relay`). | node-authored `c52d37a` | `c52d37a` (same commit — lives in the web repo) | node smoke-tested 2026-09-08 (krea ×3 batch, h3 sequence relay) | node | Not a node↔web twin change — it's Hermes tooling that happens to live in the web repo. No web-runtime coupling; the web SPA never reads it. Listed so the ledger reflects what's in `.claude/skills/`. |
| **`studio-image-prompt` Claude Code skill** — `AI_One_Studio/.claude/skills/studio-image-prompt/`. Per-model prompt dialect for the ONE STUDIO image tools: Krea2 (faithful prose) / Z-Image (rich prose) / Klein (Flux prose) / SDXL (tags + weights) / Anima (anime prose, no lens terms) / Qwen (edit instruction) — the format text lifted verbatim from `ComfyUI-TJ_NODE/nodes/llm/data/model_formats.json`, plus craft notes and a batch-diversity axis (rotate subject / light / lens / palette / medium so no two prompts in a set read alike). Batches return a JSON string array → `write_jobs.mjs` expands to job files. `hermes-job-json` now delegates image prompt authoring here (symmetric with H3 → `minimax-h3-prompt`). | node-authored `2f0fed0` | `2f0fed0` (web repo) | node-authored 2026-09-08 | node | Same as the `hermes-job-json` row — Hermes-side tooling in the web repo, not a node↔web twin change; the SPA never reads it. |
| **`minimax-h3-prompt` Claude Code skill bundled** — the H3 / Hailuo video-prompt author (storyboard brief + copy-ready structured reference prompt). Was a personal `~/.claude/skills/` skill; committed to **both** repos (node `8ebddde` / web `ceb0128`) for portability since `hermes-job-json` delegates H3 prompt authoring to it. `.claude/skills/` now holds **3**: `hermes-job-json` + `studio-image-prompt` + `minimax-h3-prompt` — `cp -r <repo>/.claude/skills/* ~/.claude/skills/` moves the set to another machine. | `8ebddde` | `ceb0128` | bundled 2026-09-09 | node | Same as the two rows above — skill tooling in the web repo, not a twin change. |
| **Node parity for row 71's "⬇ Agent JSON" button** — the ComfyUI nodes now have the same job.json export the web got. New `web/shared/agent_job.js` (`AGENT_INPUTS_DIR`, `agentInputPath`, `downloadAgentJob` envelope helper) + a per-tool `buildAgentJob()` in each node core module (`web/krea2/core_krea2.js`, `web/zimage/core.js`, `web/music/core_music.js`, `web/minimax/core_minimax.js`), ported field-for-field from `src/tools/*/core.ts`. UI: krea2/zimage/music get one `⬇ job.json` button under Generate (krea2 t2i/i2i/identity, zimage t2i/i2i, music both engines); h3 gets the per-clip `⬇` on each prompt row → `job_<seed>_C<n>.json`, same `clipAssets`/`promptFirstFrame` resolution as the web. Image paths → `/Users/hermes/.hermes/render-queue/inputs/<filename>`. Filenames (node `e8a46c3`): `Krea2_<t2i\|i2i\|id>_<YYYYMMDDHHmm>_<seed>.json`, `Zimage_<mode>_<stamp>_<seed>.json`, `MMH3_<t2va\|fl2va\|ref2va>_<stamp>_<seed>_<clip#>.json` — web's `<tool>_<mode>.json` / `job_<seed>_C<n>.json` is the older scheme; **open port web→node** if the web should match. | `1077388`, `e8a46c3` | (row 71) | 2026-09-10 | node | Closes the "노드에 다운로드 버튼이 없다" gap. No web change — web already shipped this in row 71; this row is node catching up. Same music caveat as row 71 (no real Hermes music sample yet). |
| **H3 PDD Acc → core-native LoRA; drop PDD-Acc + H3 Cache packs** — ComfyUI v0.35.0 (PR #15908 "Support PDD LoRA") moved the progressive-distillation head bank into core `FinalLayer`, so PDD loads as a plain model-only LoRA on a normal scheduler. **Web needs the same migration** in `src/tools/minimax_h3/`: (1) `graphBuilder.ts` — `turbo==="pdd"` → emit `LoraLoaderModelOnly {model, lora_name: pddFileForMode, strength_model: pddLoraStrength}` instead of `MiniMaxH3PDDAccApply`; sampler `sigmas` always from `BasicScheduler` (drop the `[pdd,1]` wiring); keep euler + shift 12/3 + steps=nfe. (2) `core.ts`/`settings.ts` — PDD file picker draws from the **regular loras list** not a `pdd_acc` scan; drop `pddHeadStrength` + the `head strength` field; `effectiveTurbo` drops the `MiniMaxH3PDDAccApply` install gate; `TURBO_MODES` pdd `node: null`. **The raw alibaba-pai `Acc-8Step.safetensors` does NOT load** (DiffSynth key naming → 0 patches, silent base-model render) — user must use the ComfyUI-converted `MiniMax-H3-{FL2VA,Ref2VA}-Acc-8Step_pruned_comfy.safetensors` (kijai). (3) Drop **`ComfyUI-MiniMaxH3-Cache`** (`h3cache` block-cache option + `MiniMaxH3Cache` node) everywhere — it import-time global-patches `MiniMaxH3Model._forward` with a pre-#15908 fork and breaks **every** H3 render on core 0.35+; `ApplyMiniMaxH3FirstBlockCache` is the surviving step-reuse cache. Clamp legacy `h3cache` state/presets → `none`. Node also stripped both packs from `install_requirements.*` / `dependency_check.py` / probe lists / README. | `e77dfc9` (v1.25.1) | `<pending>` | node render-verified 2026-09-11 (`_pruned_comfy`, 8-step, no `lora key not loaded`); web tsc + build clean, browser e2e pending a ComfyUI ≥ v0.35.0 | node | **Web mirror done:** `graphBuilder.ts` — `turboEffective` pdd branch drops the `MiniMaxH3PDDAccApply` avail gate; `N.pdd` → `LoraLoaderModelOnly {model, lora_name: pddFileForMode, strength_model: pddLoraStrength}`; sampler `sigmas: [N.sched, 0]` always (dropped `[N.pdd,1]`); `N.cache` (h3cache branch) removed; euler + shift 12/3 kept. `core.ts` — `pddHeadStrength` / `cacheThreshold`·`Start`·`End`·`MaxSteps` off the interface + `defaultState`; `BLOCK_CACHES` loses `h3cache`; `migratePipelineState` maps only `useFirstBlockCache`→fbcache and clamps `h3cache`→`none` on every load; `applyPreset` clamps `preset.cache==="h3cache"`→`none`. `settings.ts` — PDD picker draws from `modelData.loras`; note points at `…_pruned_comfy`; `cache_*` off the config save/load. `view.ts` — PDD "Head strength" field + h3cache settings block removed, notes reworded. `api.ts` — `pdd_acc` field + `MiniMaxH3Cache` / `MiniMaxH3PDDAccApply` off the node lists + `cache_*` off `MmhConfig`. `MiniMaxH3MemoryEfficientSageAttentionPatch` is from **kjnodes**, not the Cache pack — dropping Cache does not touch the default `memeff_sage` forward. |

## Upstream (ComfyUI-TJ_NODE — the third repo)

| item | TJ_NODE commit | notes |
|---|---|---|
| One-Take seam artifact — measured write-up | `400d68b` | `TJ_H3_LatentContinuation` hard-splices with no feathering; frames 39–42 of every continued clip break. STUDIO_ONE hides it by trimming 43 frames on manual stitch (a workaround). Real fix belongs here; proposed directions in that write-up are unverified. |
| Free Text Encoder VRAM port | `075b9d1` | `SPEC_FREE_TEXT_ENCODER_VRAM_PORT.md` in the node repo. |
| MiniMax H3 Audio Lock port | `075b9d1` | `SPEC_MINIMAX_H3_AUDIO_LOCK_PORT.md` in the node repo. `HANDOFF_REGISTRY.md` records the split. |

## SPEC_MINIMAX_H3_CONTINUE_AND_EXTEND.md

| § | item | node | web | verified | origin | notes |
|---|---|---|---|---|---|---|
| 1 | `POST /minimax_h3_one/clip_last_frame` + `getClipLastFrame` | `v1.21.0` | `54a5634` | 2026-09-01 | node→web | frames-glob then ffmpeg `-sseof -1` fallback → input/. Web = client fn only (route is the node's). Live-verified: returns `mmh3_seed_*.png`. |
| 2 | Prompt Edit "Continue generating the clip" (gallery frame picker, before-clips off + override greyed, `_resumeSnapshot` restore, clear on set-load) | `v1.21.0` | `54a5634` | 2026-09-01 | node→web | Resume seed only — prompt-list resume already worked via set + enable toggles. Web keeps `_resumeSnapshot` untyped (`as any`), matching the node. |
| 3 | Gallery `Extend` (3-button bar, prompt-only popup, LLM Review/Auto, `ctx.runExtend` → FL2VA continuation → auto-stitch `[source, continuation]`, §5 meta) | `v1.21.0` | `54a5634` | 2026-09-01 | node→web | Traps: read `rs._extendFrom` not `state._extendFrom`; probe source dims (`getVideoInfo`) → megapixels/aspect or the concat 0-bytes; `/stitch` scale+pad is node-side (also fixes manual Combine — nothing carried to web). Live-verified: real 0.2MP/192f render → 15s playable EXTENDED video, meta `{stitched, extended}`. Web extracted `reusePrompt` body → shared `applyClipSettings`. |
| 4 | User pipeline presets carry the full left-panel recipe (`RECIPE_KEYS`) | `v1.21.0` | `54a5634` | 2026-09-01 | web→node | User reported: a saved "PDD" preset restored `turboMode` but not `pddFile` → `effectiveTurbo` fell back to none → 20 steps not 8. Built-in 6 unchanged (axes only); `matchPreset` unchanged. Web `RECIPE_KEYS` omits node's `slaTurboLora` (no such field web-side — SLA turbo LoRA is a plain LoRA-list entry). Live-verified: saved preset carries steps/sampler/pddFile in the node config. |
| 5 | `_mmh3_last` / `_mmh3_drop_stale_last_frame` were referenced but never defined — NameError on every mmh3 render, node last-frame output broken | `v1.21.0` | n/a | 2026-09-01 | web→node | Web has no Python backend — all `/minimax_h3_one/*` routes are the node's. Nothing to port. |
| 6 | Reuse/clip-meta reproduces the turbo config, not just `turboMode` — `metaForVideo` now writes pddFile/pddFileReference/pddNfe/turboLora*/turboSteps/slaTurboSteps/slaTurboLora/scheduler/denoise/shift, `reuseAll` restores them | `v1.21.2` | `969944c` | 2026-09-01 | web→node | Web session found it while porting §1.21.1. Same class as §4: `turboMode="pdd"` restored without the file → `effectiveTurbo` falls back → normal steps. `reuseAll`/`applyClipSettings` already had a dead turbo-LoRA restore branch (nothing wrote the fields). Web adds pddFile/pddFileReference/pddNfe/turboLora/turboLoraReference/turboLoraStrength/turboLoraLowVram/turboSteps/slaTurboSteps/scheduler/denoise/shiftVideo/shiftAudio to `metaForVideo` + `!= null` restores in `applyClipSettings`; skips `slaTurboLora` (plain LoRA entry). No-op on existing clips (metas predate the fields); tsc clean, no live render (node-verified, web metas all null until new renders). |
| 1.21.1 | Extend carries the source's accelerator across the forced First/Last (`pddFile ||= pddFileReference`, `turboLora ||= turboLoraReference`) | `v1.21.1` | `ea4f541` | 2026-09-01 | node→web | 2-line guard in `runExtend`. Web sentinels: pddFile `"none"`, turboLora `""`. |
| 7 | Stitched mp4 `moov` atom after `mdat` → won't play on iOS Safari over HTTP. `-movflags +faststart` added to all 3 `mmh3_stitch` ffmpeg branches (covers One-Take stitch, Extend, manual Combine, chunked upscale/deblur/interpolate joins) | `v1.21.3` | n/a | 2026-09-01 | web→node | `/stitch` route is node-only. Diagnosed web-side from the atom order (user hit it on iPhone via the cloudflared tunnel): individual clips `ftyp/moov/free/mdat`, stitched `ftyp/free/mdat/moov`. Node-verified. **Old stitched files stay broken — re-run to fix.** |
| 8 | Gallery post-process hangs when the WS drops (mobile background throttle over the tunnel) — chunk's `queuePrompt` waited only on WS `execution_success`; job finishes, result in `/history`, promise never resolves → stuck on "preparing chunk 1/N" | `v1.21.4` | `24458d0` | 2026-09-01 | web→node | Diagnosed web-side from an iPhone. `queuePrompt`'s fresh-submit path now runs the same 5s `/history` poll the reattach path already had (node added it as defence-in-depth — its WS is usually stable). **Q2 done, web-only** (`514b6dc`): `opts.onPoll` fires each poll tick while running → gallery readout shows "still working (connection quiet)…", main render status likewise, so the label isn't frozen while only the poll reports. **Intentional divergence**: node does not mirror `onPoll` — its WS normally drives `progress` fine (rare-path safety net only), so the plumbing isn't worth it there. If the node's WS drops mid-chunk the poll still resolves cleanly, just without intermediate label ticks. |
| 9 | Upscale/Deblur chunk size — per-method **duration** rule, not byte budget | `v1.21.5` | `431be92` | 2026-09-01 | web→node | User rule: **RTX VSR / RTX deblur** = whole file `<15s`, 15s chunks `>=15s`. **Upscale model** = whole file `<10s`, 5s chunks `>=10s`. deblur+model → model rule. Interpolate keeps byte-budget (no rule given). Web: `runPost` gained `chunkPlan(durationSec)=>chunkSeconds`; byte-budget stays as the video_info-unreadable fallback. Node v1.21.5 mirrors (`RTX_PLAN`/`MODEL_PLAN`), sim-verified both sides. |
| 10 | Post-process (upscale/deblur/interpolate) output meta carried the **source's** w/h/frames/fps — `runPost` spreads `{...v.meta}` verbatim for Reuse (§5) but never corrected geometry. Now probes the finished file (`video_info`), overwrites w/h/frames/fps/durationSeconds, drops source `elapsedSec`, adds sourceW/sourceH; ⓘ shows "⚙ upscale (from WxH)" + "N frames @ Mfps" | `v1.21.6` | `4a9d683` | 2026-09-01 | web→node | Found on iPhone: 2× upscale of a 1440×960 clip → real 2880×1920, meta said 1440×960. Prompt/seed/pipeline still the source's (Reuse rebuilds the original). Existing files keep stale meta. |
| 11 | Single-shot post-process survives a page reload / iOS tab discard — job stashed to localStorage on queue, `resumePostJob()` reattaches via `queuePrompt({existingPromptId})` + `/history` on gallery open (probe geometry → write meta → discard input copy → refresh → toast), else waits. Stale stash (no ComfyUI record) is dropped. Progress line: "keep this tab open". | `v1.21.7` | `dcb2858` (+ `825137f` warning line & `keepTabAlive`) | 2026-09-01 | web→node | User asked for it (not deferred). Same reattach machinery as the main render. **Chunked jobs NOT resumable** (loop + slicing can't replay) — warning line only. Web adds `keepTabAlive` (silent-audio) around runPost/runStitch; node skipped it (desktop tabs reload, aren't discarded). §8's `/history` poll (node v1.21.4 / web `24458d0`) already covers "WS drops, tab alive". |

## SPEC_MINIMAX_H3_ENHANCE_APPLY_MODES.md

| § | item | node | web | verified | origin | notes |
|---|---|---|---|---|---|---|
| 1 | Enhance-result review: 3 apply modes (One Prompt / Auto Split / Use selected) chosen after the model answers, centred caption over the buttons, mode block above Discard/Apply. Manual mode = click cards to pick; selected 2px brand border, off = grey border + inset black scrim. Shot-count mismatch warning for target "all". | `v1.22.0` | `ad25b53` | 2026-09-01 | user | Cards always preview; only apply method chosen last. `reviewSel` seeded all-ticked. |
| 2 | `parseBrief`: broadened `TAIL_RE` to catch the structured audio section (`overall_soundscape:` / `non_diegetic_music:` + markdown/underscore/hyphen variants), added `ECHO_RE` + `stripEcho()` to drop the model's trailing instruction / vision-analysis echo from every block. | `v1.22.0` | `ad25b53` | 2026-09-01 | user | Was only matching `Ambient sound:` / `Music:` → structured briefs glued the tail onto the last shot. |
| 3 | Prompt Edit per-field Undo / Clear — header / sound-music / shot fields each get an `Undo` `Clear` pair top-right of the label. Clear empties only that field (writes through to state); Undo steps back through focus-captured + pre-Clear values (per-field stack, max 15). Shot field's stack resets on clip switch. | `v1.22.1`+`v1.22.2` | `c9efc06`+`885c5e3` | 2026-09-01 | user | `undoClearBtns(ta, write)` helper; `resetStack()` from `loadSelected`. v1.22.2: `paint()` dims Undo (empty stack) / Clear (empty field) to muted, white when actionable; `row.paint` called from refreshFraming/renderAll/syncCommon. Web-verified both. |

## Prompt Edit drag-reorder + resizable preview (no spec — pure UI)

| item | node | web | verified | origin | notes |
|---|---|---|---|---|---|
| Drag-reorder the CLIPS list (splice `state.prompts`, open clip follows the move); row label → fixed `N - Clip Prompt #N` (was a truncated content preview); drag-reorder attached media in 3 places (left-panel reference image grid, Prompt Edit image grid common+override, shared 3-slot video/audio tiles — image number = `<Picture N>` token so reorder changes what the prompt refers to); main-node preview box drag-resizable from its bottom edge (current height = max, shrinking grows the PROMPTS list, `localStorage mmh3_preview_h` clamp [220,620], node total height unchanged) | `v1.23.0` (`8c8702c`) | `3f4777f` | 2026-09-02 | user | Node browser-verified the clip drag (Clip 2→pos 1, content followed). Web browser-verified all four. Web preview handle switches `flex: 0 0 <px>`, promptWrap (flex 2 1) absorbs; clamp [220,720] (viewport-responsive, no 620 cap); key `aos_mmh3_preview_h`. Media reorder helper `dragReorder()` uses private dataTransfer type `application/x-mmh3-slot` to not collide with drop-to-upload. No wire changes. |

## SPEC_MINIMAX_H3_INLINE_POSTPROCESS_META.md

| § | item | node | web | verified | origin | notes |
|---|---|---|---|---|---|---|
| 1 | `buildClipGraph` meta records the frame ops it actually wired: `deblur` (strength\|null), `upscale` (`{method:"model",model}` \| `{method:"rtx",scale,quality}` \| null) | `v1.23.1` (`8cada97`) | `8996bbe` | 2026-09-02 | node | Set inside the same `if` blocks that add the nodes, not recomputed from raw state. Node + web both verified in-browser via module import: none→`{null,null}`, deblur-only→`{"HIGH",null}`, model→`{null,{method:"model",model}}`, rtx+deblur→`{"LOW",{method:"rtx",scale:3,quality:"HIGH"}}`. |
| 2 | Generation + stitch save paths re-probe the output (`reconcileGeometry(meta,file,{keepFrames})` helper): per-clip when `built.meta.upscale`; One-Take with `keepFrames:true` (overlap-computed durationSeconds); Extend plain. `sourceW/H` = pre-op size, only on a real size change | `v1.23.1` | `8996bbe` | 2026-09-02 | node | **Node now verified with a real render** — PDD 8-step / 0.2MP, Upscale = model ×2 + Deblur MEDIUM → sidecar `w/h` 1088×704, `sourceW/H` 544×352, `deblur:"MEDIUM"`, `upscale:{method:"model",model:"RealESRGAN_x2.pth"}`, no `postProcess`. Web still code-only. `reconcileGeometry` gained `{ noSource }` in node v1.23.2 (for the `_raw` clip — see §6). |
| 3 | `reuseAll` (web: `applyClipSettings`) restores `deblurStrength` / `upscaleMode` / `upscaleModel` / `rtxScale` / `rtxQuality` — **only when `!meta.postProcess`** (inline pass); a gallery post-processed file rebuilds the §5 un-processed original | `v1.23.1` | `8996bbe` | 2026-09-02 | node | Node verified with a synthetic inline-shape meta. Web verified against a real node-generated inline clip (`deblur:"MEDIUM"`, `upscale:{method:"model",model:"RealESRGAN_x2.pth"}`, no `postProcess`): ↩ Reuse → `deblurStrength:"MEDIUM"` / `upscaleMode:"model"` / `upscaleModel:"RealESRGAN_x2.pth"`. |
| 4 | Gallery `writePostMeta` (web: `patchPostMeta`) gains `postInfo` (threaded runPost→savePostJob→finishPost/patchPostMeta, so resumed jobs write it): `postProcess` now names every stage (`"deblur + upscale"` / `"rtx upscale"` / `"interpolation"`), structured `deblur`/`upscale`/`interpolate` keys written, `sourceW/H` only on real change | `v1.23.1` | `8996bbe` | 2026-09-02 | node | **Node now verified with a real gallery pass** — deblur MEDIUM + model ×2 on a clip → new file meta `postProcess:"deblur + upscale"`, `deblur:"MEDIUM"`, `upscale:{method:"model",model:"RealESRGAN_x2.pth"}`, `sourceW/H` 1440×960 → `w/h` 2880×1920, `postSource` set. Card shows `⇪`+`✧`. Web still code-only. Blocked earlier by §8's TDZ bug (the go button was dead); fixed in node v1.23.2. |
| 5 | Thumbnail card bottom-left badges: `⇪` upscaled / `✧` deblurred (both can show), 18px `rgba(0,0,0,0.6)` chips; read from the same meta keys inline or gallery. ⓘ tooltip `⚙` line synthesises a label from `m.deblur`/`m.upscale` when `m.postProcess` absent | `v1.23.1` | `8996bbe` | 2026-09-02 | node | Spec: `SPEC_MINIMAX_H3_INLINE_POSTPROCESS_META.md`. Node + web verified in-browser. Web against a real node-made inline clip: `⇪` "Upscaled — RealESRGAN_x2.pth" + `✧` "Deblurred — strength MEDIUM"; ⓘ tooltip `⚙ deblur + upscale (from 544×352)` + post-op `1088×704`. |
| 6 | **"Also save the clip before deblur / upscale"** left-panel toggle (`state.saveUnprocessed`) — inline pass writes a second `_raw` file straight off the decode; sidecar `unprocessed:true` + `processedSibling`, own resolution, no `sourceW/H`, no badges; never in `clipRecords` (no stitch / last-frame chain). `buildClipGraph` adds `N.videoRaw`/`N.saveRaw` on `preProcImages` + returns `rawVideoNode`. `reconcileGeometry` gains `{ noSource }`. | `v1.23.2` (`762b011`) | `b627622` | 2026-09-02 | user | Node verified with a real render. Web verified: `buildClipGraph` writes the raw nodes only when `saveUnprocessed && (deblur||upscale)` (raw `CreateVideo` sources `MM:decode`, `_raw` prefix); the accordion checkbox appears only when deblur/upscale ≠ none and persists; and against the node's real `MMH3-2_clip001_raw_00001` clip the gallery shows **no badges** on the raw file while its `_00002` sibling shows `⇪`+`✧`. Web toggle lives in the Upscale accordion body; `state.saveUnprocessed` added to `MinimaxState` + `defaultState`. |
| 7 | Gallery thumbnail interpolation badge `⇄` (title `Interpolated — Nfps` from `m.interpolate.targetFps`). Badge glyphs are now `⇪` / `✧` / `⇄`. ⓘ `⚙`-line synthesis appends `"interpolation"` when `m.interpolate` set and `m.postProcess` absent. | `v1.23.2` | `b627622` | 2026-09-02 | user | Node verified with a faked meta. Web: same badge block that renders `⇪`/`✧` (verified §5) + one push; logic checked — `⇄` "Interpolated — 48fps", glyph order `⇪`/`✧`/`⇄`, synth label `"deblur + rtx upscale + interpolation"`. No interpolated clip on disk to render against yet. |
| 8 | **Bug fix (present since v1.20.0)** — `ui_gallery_minimax.js refreshUpBar()` computed `const ready` from `noUpscale` / `deblurOn` / `deblurOk` three lines before their `const` declarations → `ReferenceError` every call → `upGoBtn.disabled` never set → the gallery `⬆ Upscale` / `✦ Deblur` buttons were permanently disabled. Reordered the three consts above `ready`. | `v1.23.2` | n/a | 2026-09-02 | node | Node-only bug. The web's equivalent (`refreshUpscaleAvailability` in `galleryOverlay.ts`) already declares `deblurOn`/`deblurOk`/`isNone`/`upscalerOk` **before** the `upMissing` check that uses them — no TDZ, nothing to fix. Web's post-process buttons were never stuck. |

## SPEC_MUSICMAKER.md — MusicMaker ONE STUDIO (TJ)

Third family axis (music) after video (H3) and images. ONE node, TWO engines
(`MiniMax Music 3` native + `Ace-Step 1.5` 3×SamplerCustom). SUNO-style UI: left
compose panel, always-visible right playlist, bottom player bar. Node source is
`web/one_node_music.js` + `web/music/*` + `nodes.py` music section. Web mirror not
started yet — this section seeds it.

**Web mirror landed `075b9d1`** — `src/tools/music/` (core / graphBuilder / api /
comfyClient / view), a 1:1 port of `one_node_music.js` + `web/music/*`. The `.mmm-*`
style block, SUNO layout, ENGINE_FIELDS engine swap, generation queue, Krea2 cover
flow and the 3-backend LLM path (all via `POST /music_one/llm/run`, `comfy` backend
queues a `TextGenerate` graph) are carried over verbatim. Registered next to MiniMax
H3 under the renamed **Media Generator** menu group. Backend `/music_one/*` routes
are the node's.

**Live (node up, 2026-09-06):** playlist / covers / meta / config read paths
browser-verified against real tracks; `/music_one/llm/run` POST fires with the
right body; engine order (Ace-Step first + default) and sequential sampling-stage
cascade verified. Dev proxy needed `/music_one` added to `vite.config.ts`
`comfyPaths` (`9429fc2`) — was hitting the SPA fallback. Still to eyeball manually:
the ✨ LLM result landing in the lyrics/style box (verbatim mirror of the node
path; server salvage for the reasoning model verified node-side).

| item | node | web | verified | origin | notes |
|---|---|---|---|---|---|
| Album cover generated as one set with the track (`state.makeCover`, default on) — `generate()` fires `makeCover(meta,audio)` after save_meta; Krea2 t2i graph (640², 8 steps), `cover_prompt` LLM role, writes `coverImage` to the track meta, `subfolder one_music/covers` | v1.24.x | `075b9d1` | pending | user | "어차피 생성하면 한 세트처럼 동작되야 하니까." Best-effort (`.catch(()=>{})`) — a cover failure never blocks the audio. |
| Regenerate cover — playlist row ⋯ menu → "썸네일 다시 만들기". No prompt input; `regenCover(t)` loads the track meta, assigns a fresh random seed, re-runs `makeCover`. Status feedback in `statusEl`. | v1.24.x | `075b9d1` | pending | user | "프롬포트 이런거 쓰지말고 그냥 썸네일 재생성 하나로 알아서 되게." `makeCover` hardened: falls back to a synthesised prompt when `cover_prompt` LLM returns empty. |
| Full design pass — every surface reskinned to SUNO tone, zero shared gray primitives. Playlist rows are cards (`.mmm-row`/`.mmm-cover` 52px w/ ▶❚❚ overlay, `.mmm-acts` fade-in, `.mmm-cb` hidden until hover); player bar `.mmm-bar` translucent+blur, `.mmm-pp` brand play circle; ✨ = `.mmm-spark` filled brand circles; 심플/고급 + engine + settings-tabs = `.mmm-seg`/`.mmm-tabs` (purple active pill); form fields `.mmm-fld`/`.mmm-sel` (custom arrow); 고급 params in `.mmm-acc` cards w/ `.mmm-grid2`/`.mmm-grid3`; `.mmm-go` gradient generate button; `.mmm-menu` icon+label popup (⋯ + 📚 presets); `.mmm-ov` full overlays (settings / info / ⛶ big-edit). All klein/ui_common imports dropped. | v1.24.x | `075b9d1` | node-verified 2026-09-06 | user | User rejected raw-primitive v1 twice ("회색 버튼 이거 뭐야", "테스트 하고 싶은 마음이 안생김"). Local helpers `fld/sel/fieldCol/seg/rangeRow/sectionHead/popMenu/checkRow/bigEdit/presetMenu` in `one_node_music.js`. 📚 presets wired to `/{lyrics,style}_presets` CRUD. |
| **Bug fix** — `music/core_music.js` `el()` set `className` via `setAttribute("className", …)` (literal attr, no styling). Now `node.className = v` for `className`/`class`, plus `value` handled as a property. Every `.mmm-*` class depended on this. | v1.24.x | n/a | node-verified 2026-09-06 | node | Web twin is React — N/A. Symptom was subtle: inline-styled elements looked fine, class-only styling silently no-op'd. |
| **English-only + sibling-pattern pass** — user rejected KO/EN i18n effort for this node ("just English"). Whole music surface is now English; every icon is a monochrome glyph except the ✨ generate star (per "아이콘 형태의 특수문자는 단색형태만 사용, 노란별 제외"). Section-header actions are text buttons `Reset / Preset / Reroll / Expand` + `✨` (`.mmm-tb`), not icons. Model pickers in Settings + LoRA use `searchSel()` (filter box + native select, mirrors `klein/ui_common.js loraSelect`). LoRA is an add/remove list (`+ Add LoRA` / `✕`), not fixed 3 slots. Local-LLM model is a `searchSel` fed by `/tj_studio_one/llm/models` `.gguf`. Player ▶/❚❚ is inline SVG in a rounded-rect (no circle — triangle can't optically center in one). Generate row = `[▶ Generate][■ Stop]` in that order (matches `one_node_krea2.js`). | v1.24.x | `075b9d1` | node-verified 2026-09-06 | user | Reference: the user's own image nodes. `nodes.py _music_playlist` row now also returns `caption` (style prompt) for the SUNO-style 2-line row; needs a restart. |
| **Playlist row = SUNO layout** — `[checkbox / ☆ favorite stacked] [cover w/ duration badge] [title + engine badge (MiniMax/Ace-Step) / style-prompt line] [↺ ⓘ ↓ ⋯]`. Cover click → info popup (big cover + lyrics + params). Title click = play/pause **toggle**, double-click = restart (200ms debounce). Selection bar sits **above** the search row, always visible, Delete/Clear disabled at 0 selected. | v1.24.x | `075b9d1` | node-verified 2026-09-06 | user | Row helpers `togglePlay`/`restartPlay`; `.mmm-eng` badge; `.mmm-dur` overlay on cover. |
| **Player: single vs continuous toggle** — `⟳` button in the player bar (after Next). `state.continuous` (default true, persisted). `ended` → next track only when continuous; single = stop after current. `loadPlaylist` re-resolves `curIdx` by filename so a mid-generation list refresh doesn't lose the playing track. Completion no longer auto-plays if the user is already listening to something (`wasIdle` guard). | v1.24.x | `075b9d1` | node-verified 2026-09-06 | user | |
| **Live generation card** — hitting Generate puts a card at the top of the playlist immediately: **cover is generated first** (`coverImage(meta)` — split out of the old `makeCover`) so the pending card has a face, then the music graph runs. Card shows stage (`Making cover… → Queued… → Encoding prompt → Sampling → Decoding audio → Saving → Finishing…`) + a live % bar, driven by `progress` / `executing` WS events filtered by `prompt_id`; `paintPending()` updates the DOM in place (no re-render). On completion the card is replaced by the real track (`loadPlaylist`); on error it turns red with a ✕ dismiss. Other tracks stay playable throughout. Bug fixed: `res.node_errors` is often `{}` on success — was throwing; now checks `Object.keys(...).length`. | v1.24.x | `075b9d1` | node-verified (card + error path) 2026-09-06; full run pending user restart | user | `pendingGen` state + `pendingCard()`/`paintPending()`. `.mmm-row.gen` CSS (sheen animation on `.mmm-cover.busy`, `.mmm-prog`/`.mmm-stage`). |
| **Download = tagged MP3** — `GET /music_one/download?filename=&subfolder=` transcodes to MP3 via `_ffmpeg_exe()` with embedded ID3v2: cover art (`-disposition:v attached_pic`, mjpeg), `title`, `artist=AI ONE STUDIO`, `album=MusicMaker`, `comment=<style caption>`, `encoded_by=<engine>`, `lyrics=<full lyrics>`, `MMM_SEED`. Cached to `output/one_music/.export/<title>.mp3`, regenerated when audio/meta/cover is newer. Frontend fetches as blob → `<a download>` `.mp3`. | v1.24.x | `075b9d1` | **node-verified 2026-09-06** (route returns audio/mpeg, `.export/*.mp3` on disk) | user | Uses `imageio-ffmpeg` (already a dep). |
| **Bug fix — music meta never read back** — `_read_json_meta`'s `_VALID` signature tuple was image-only (`v/prompt/w/h/mode/favorite`). Music meta has none of those keys, so every meta file written by `save_meta` was silently rejected on read → no cover on playlist rows, empty style-prompt line, dead Reuse/Info. Added `engine/caption/lyrics/seconds` to `_VALID`. | v1.24.x | n/a (React) | node-verified 2026-09-06 (full generate → cover + caption + title on the finished row) | node | This gated the entire meta layer for the music node. |
| **Node size + resizable left panel + Title field** — outer node size now `1280×994`, matching MiniMax H3 exactly. `root` is `position:relative;width/height:100%;padding` (was `position:absolute;inset` which overflowed right/bottom because the DOM-widget host isn't `position:relative`). Compose panel has a `col-resize` drag handle between it and the playlist; `state.leftW` clamp `[380, 760]`, persisted. New **Title** input at the top of the compose panel; `state.title` feeds the finished track's title and the Lyrics ✨ (empty lyrics + Title present → `lyrics_from_title` role). LoRA list starts empty (placeholder `none` rows filtered on load); rows are `[searchSel: filter / select] [col: ✕ / strength]` all gap-4. Live-progress `%` now also driven by the `progress` event's own `node` field, not just `executing`. | v1.24.x | `075b9d1` | node-verified 2026-09-06 | user | |
| **Batch 3 (all browser-verified after a real `POST /api/manager/reboot`)** — restart endpoint for this ComfyUI Desktop + Manager V3.41 is **`POST /api/manager/reboot`** (not GET; the thrown "Failed to fetch" *is* success — the server drops the socket). Fixes verified end-to-end: (a) **`SaveAudioAdvanced` V3 combo** — the sub-option is a **dotted key** `"format.quality"`, not a sibling `quality` and not a nested array `["mp3",{quality}]` (both of those validate but `.execute()` then dies with "missing required argument: format"). mp3 gen now completes → `MMM_00008.mp3`. (b) **`_openrouter_chat`** hardened — no more `'NoneType' has no attribute 'strip'`; returns a clean `OpenRouter <code>: <detail>` on error, handles `content` as string/list/missing. (c) **`llm_model` split** into `llm_model` (local GGUF) + `llm_or_model` (OpenRouter id) so switching backend doesn't feed a GGUF path to OpenRouter. (d) **local GGUF LLM** now calls the real `TJ_PromptEnhancer().enhance(get_name, raw_prompt, model_backend="GGUF / llama.cpp", gguf_model, system_prompt_override, …)` — returns text. (e) **insertAtCursor** — `focus({preventScroll})` + `setSelectionRange` after the input event; chips/tags `onmousedown:preventDefault` so the caret stays put and the field doesn't scroll to the bottom. (f) **`seg()._sync(val)`** — `reuse()` / programmatic engine change now repaints the topbar pill. (g) **per-engine independence** verified: MiniMax title/lyrics/bpm survive a round-trip through Ace-Step untouched. (h) `console.info("[MusicMaker] queued …")` on every /prompt so the browser console shows the queue. | v1.24.x | `075b9d1` | **node-verified 2026-09-06** | user | Still TODO: H3-style multi-item queue. |
| **Big compose rework (batch)** — (1) `seg()` self-repaints on click → fixed the top engine / Simple-Advanced purple pill not moving. (2) Compose panel split: scrollable body + **pinned bottom block** (`status text · seed / seed-mode · Generate / Stop`) that never scrolls. (3) Lyric tags & style chips `insertAtCursor()` — insert where the caret is, not appended to the end. (4) `Reset` header button now clears + re-renders (was a no-op). (5) **Cover Info** button next to Title → small popup for a plain-language cover description → `state.coverBrief`; `coverImage()` sends `coverBrief` OR title+lyrics to the LLM and never uses a hard-coded fallback (LLM down = no cover). (6) No-cover thumbnails show bold **MM / ACE** (`coverPlaceholder()`), everywhere (row, big info cover, pending card, player mini-cover). (7) `SaveAudioAdvanced` is a V3 dyn-combo: `saveAudioNode()` only sends `quality` for mp3/opus (with format-correct default) and drops the non-existent `wav` — fixes `Required input is missing: quality`. (8) MiniMax gets **BPM slider + scale + time-sig** above Length; they ride into the caption LLM context and `commonMeta`. (9) **Per-engine independent state** — `switchEngine()` stashes the active engine's `ENGINE_FIELDS` into `state.engineStash[eng]` and restores the other's, so MiniMax and Ace-Step never share a field; `reuse()` jumps to the track's engine then fills. (10) **LLM-busy overlay** — `llmBusy(wrap)` puts a blurred "● writing…" cover on the lyrics/style box while its ✨/Reroll LLM call runs. (11) 3rd LLM backend **ComfyUI TextGenerate** (native node, CLIP/GGUF via CLIPLoader + type); Settings LLM tab shows it. `music_llm_run` local branch rewritten to the real `TJ_PromptEnhancer().enhance(...)` signature (was calling non-existent `prompt=/model=` kwargs → silent fallback → "GGUF not found: None"). | v1.24.x | `075b9d1` | frontend node-verified 2026-09-06; **backend (llm_run local/comfy, saveAudioNode) needs a real restart** | user | Still TODO: H3-style **generation queue** (multiple queued, not one-at-a-time). |
| **Generation queue (H3-style, in the playlist)** — `pendingGen` (single) → `genQueue` array. Every ▶ Generate snapshots the whole panel (`JSON.parse(JSON.stringify(state))`) and appends one job; a single `runQueue()` worker drains FIFO (ComfyUI serialises the real runs). Each job is a `.mmm-row.gen` card at the top of the playlist with its own stage/%/cover, driven by `progress`/`executing` WS events filtered by that job's `prompt_id`. `processJob(job)` runs the job's own caption + lyric LLM from `st = job.snap` (via `llmOnce()` — no panel overlay), cover, submit, poll, save, `loadPlaylist`, then removes itself from `genQueue`. **✕ on every gen card**: queued (not started) → drop from queue; running → `/interrupt` that one run + drop, worker moves to the next (`processJob`'s poll loop bails on `!genQueue.includes(job)`). **■ Stop** = `/interrupt` + clear the whole queue. `paintJob(job)` finds the card by `data-job-id`. Editing the panel after clicking never leaks into an already-snapshotted job. | v1.24.x | `075b9d1` | **node-verified 2026-09-06** (3 jobs queued → cards + ✕; cancel queued; cancel running → next starts; Stop clears all; a queued job completed with an LLM title "Polaroid Haze") | user | Web twin: mirror — snapshot-per-click FIFO, cards in the list, per-card cancel. |
| **Playback stops when the node goes away** — the player is a detached `new Audio()`; closing/replacing the workflow left it playing. `self._mmmStop()` (pause + clear src + load) is stashed on the node; `nodeType.prototype.onRemoved` calls it (chained with any prior onRemoved). | v1.24.x | n/a | node-verified 2026-09-06 | user | React unmount handles this already on the web side. |
| **Configurable save folder** — Settings header now has **Save folder (under ComfyUI output/)** → `save_subfolder` in config + `state.saveSubfolder`. Frontend: `SUBFOLDER` const replaced by `SUB()` = `state.saveSubfolder` sanitised, fallback to default `one_music`; every playlist/meta/delete/download/cover call and the graph builder's `folder`/covers path use it; `loadPlaylist` passes `&subfolder=`. Backend `_music_playlist` reads `?subfolder=` → config `save_subfolder` → `MUSIC_SUBFOLDER`. Changing it in Settings reloads the playlist. Covers always go in `<folder>/covers`. | v1.24.x | `075b9d1` | node-verified 2026-09-06 (pending restart for the backend playlist route) | user | Web twin: same — one configurable root, playlist + save both read it. |
| **Song / Instrumental toggle** — explicit segmented control above the Lyrics section (`state.instrumental`, in `ENGINE_FIELDS` + `defaultState` false). Instrumental mode: hides the lyrics box + lyric ✨/Reset/Preset/Reroll + the Vocals gender/delivery/tone section, shows a note, sets `vocalGender = "instrumental (no vocals)"`. `generate()` forces `state.lyrics = ""` and skips every lyric LLM call; `vocalHints()` returns `{vocal_gender:"instrumental (no vocals)"}`; `commonMeta.instrumental = !!state.instrumental || !r.lyrics`. Replaces the old implicit "empty lyrics box = instrumental" (which still auto-generated lyrics from a brief). Also: `makeCover` default confirmed `true` (was only contaminated to `false` in this session's own test localStorage). | v1.24.x | `075b9d1` | **node-verified 2026-09-06** (toggle hides/shows UI; graph payload: lyrics `""`, caption gets "instrumental, no vocals", meta.instrumental true) | user | Web twin: add the same explicit toggle; don't infer instrumental from an empty field. |
| **Vocal / BPM / key / time-sig now apply without ✨** — the dropdowns/sliders only reached the model when the caption LLM (✨ or Reroll) wove them in, so a user who typed their own caption got nothing. Two fixes: **(1)** `graph_builder_music.js` `captionWithHints(caption, state, includeMusical)` appends the non-`auto`/non-empty settings to the caption text at graph-build time, with per-token dedup (skips a hint whose distinctive keyword is already in the caption). MiniMax (`MiniMaxMusic3TextEncode.caption`) gets vocal + BPM + key + time-sig (it has no structured fields); Ace-Step gets **only vocal words** in `tags` — its BPM/key/time-sig already go to `TextEncodeAceStepAudio1.5`'s own inputs. **(2)** the **Reroll** button (`one_node_music.js` ~917) was passing only `{lyrics, variation}` — now passes `{chips, ...vocalHints(), variation}` like ✨ does. **(3)** `caption_minimax.md` / `caption_acestep.md`: "a context field **overrides** any conflicting detail in the brief or prior caption — `vocal_gender: female` + 'his voice' → 'her voice', flip every gendered word." Also new MiniMax Advanced field **`guidance (cfg_scale)`** → `state.cfgScale` → `MiniMaxMusic3TextEncode.cfg_scale` (was hard-locked at 1.7, no UI; separate knob from the KSampler `cfg`). | v1.24.x | `075b9d1` | **node-verified 2026-09-06** (graph payloads inspected: typed caption + dropdowns → hints appended; dedup when already present; Reroll body carries vocal_gender) | user | Web twin: same gap — apply structured selections at graph build, not only via the caption LLM. |
| **MiniMax vocal "wind/breath" artifact = model characteristic, not cfg** — spectrogram analysis: MiniMax audio VAE brick-walls at ~16.5 kHz and renders 5–12 kHz as broadband noise-haze (no harmonic structure) → sibilance/breath band reads as wind. Ace-Step (48 kHz, clean HF structure) doesn't. Mitigations, not cures: KSampler `cfg` **down** to 1.0–1.5 (high cfg + ConditioningZeroOut negative amplifies HF noise), `cfg_scale` **up** to 3–5, `steps` 50–80, and ideally swap the `..._pruned_int8_convrot` text encoder for a full fp16 build. | n/a | n/a | analysis 2026-09-06 | user | Informational — no code change. |
| **Model list refresh** — Settings model dropdowns were fed by `ctx.models` / `ctx.llmModels`, fetched once at mount and cached forever → a model added on disk never appeared. `renderSettings()` now re-scans `/music_one/models` on every open (`settingsOv.show()` calls it) and nulls `ctx.llmModels`; new **↻ Refresh models** button in the Settings header force-re-scans while it's open. Backend `_scan()` already `os.walk`s live — no server cache to bust. Also: per-track cover-regen shows a spinner + BRAND sheen on that row's thumbnail (`.mmm-cover.regen`, `regenCoverFn` state); status line above the seed is now yellow `#ffcf3f` bold. | v1.24.x | `075b9d1` | **node-verified 2026-09-06** | user | Web twin: same stale-cache trap if it caches a model list across a settings panel. |
| **Regenerate-cover: Auto vs Prompt** — `⋯ → Regenerate cover` now opens `regenCoverPopup(t)`: segmented **Auto ReGen / Prompt ReGen**, a prompt textarea (disabled+dimmed under Auto), `ReGenerate / Cancel`. Auto = LLM from title+lyrics; Prompt = the typed description goes through the `cover_prompt` LLM role into a Krea2 prompt. `coverImage(meta, opts)` gained `opts.brief` (`undefined`→compose-panel Cover Info, `""`→auto, string→that text). Cover Info button next to Title restyled → `.mmm-cinfo` (BRAND bg, white text, height matched to the input). | v1.24.x | `075b9d1` | **node-verified 2026-09-06** (Auto ReGen produced `K2_00008_.png`, applied to the row) | user | Web twin: mirror the Auto/Prompt choice if it adds per-track cover regen. |
| **6-combo end-to-end test batch (2×engine × 3×LLM, all browser-clicked, 60 s tracks)** — `.env` in `NODE_DIR` is now the source of truth for `OPENROUTER_API_KEY` (`_env_read`/`_env_write`, gitignored, re-read on every call — no restart to pick up a new key). Fixes found by the run: **(a)** `TextGenerate` was called with `use_default_template:false` in BOTH `comfyTextGen()` (frontend) and `_run_textgen()` (backend) → no chat template → the GGUF raw-continued the tail of the prompt string (garbage like `", slow groove."`). Now `true` — clean captions. **(b)** `_strip_thinking()` (py) + `stripThinking()` (js) added: drop `<think>` blocks, and only when the reply *opens* with an unmistakable reasoning tell, rescue a quoted/marked final draft. First pass was too aggressive (leading `#`/`-` counted as "reasoning", mangled valid markdown output) — now conservative, opener-only. **(c)** `title` + `coverBrief` removed from `ENGINE_FIELDS` (song-level, not engine-format-level) — a stale `"ACE"` title was surviving in `state.engineStash.acestep` and blocking regen. **(d)** post-gen title regen gated on new `state.titleTouched` flag (set by the Title field's own oninput) instead of `!meta.title`, so an auto/stale title always gets replaced; falls back to `cleanTitle(lyrics|caption)`. **(e)** the user's configured local GGUF `huihui-qwen35-4b` is a reasoning model that ignores `/no_think` and dumps CoT → switched `llm_model` to `Gemma-4-E4B-Uncensored` (non-thinking, clean). `_run_local` also now hard-locks output format in the system prompt + floors `max_tokens` at 2200 + caps temp at 0.4. | v1.24.x | `075b9d1` | **node-verified 2026-09-06** — 6/6 tracks: MMM_00002 MiniMax+OpenRouter, _00003 MiniMax+Comfy, _00004 MiniMax+Local, _00005 Ace+Local, _00006 Ace+OpenRouter, _00007 Ace+Comfy; all 60 s, captions+lyrics+seed+tagged-MP3 download OK | user | H3-style multi-item queue still TODO. Web twin: `useDefaultTemplate` + the reasoning-model note apply if it ever wires a local GGUF path; `.env` key store is node-only. |
| **Track Info shows the LLM used** — `commonMeta` now records the *resolved* model per backend (`llmOrModel` for openrouter, `llmClip` for comfy, else `llmModel`) instead of always `state.llmModel`; the Info panel adds a `LLM` line (`Local GGUF / OpenRouter / ComfyUI TextGenerate` + model id). | v1.24.x | `514d809` | node-verified 2026-09-06 | user | Web twin: same fix — record the model that matches the chosen backend, surface it in the track detail view. |
| **Lyric LLM now gets the real song length** — new `effectiveDuration(state)` export in `graph_builder_music.js` (duration hint in the lyrics/style brief like "3:00"/"3분" wins over the slider); `resolveCommon` uses it, and both lyric-LLM call sites (compose ✨ and `processJob` queue) pass `duration_seconds: effectiveDuration(...)` instead of the raw `state.duration`. `lyrics_from_theme.md` + `lyrics_from_title.md` rewritten with a mandatory structure-by-length table + line-count targets (sung lyric ≈ 55–70% of duration at ~3–4 s/line; a 3-min song ≈ 30–42 lines, not one verse + a short chorus; expand a short hook into the full song). `comfyTextGen` `max_length` 1400→2048. | v1.24.x | `514d809` | node-verified 2026-09-06 (syntax); full run pending restart | user | Web twin: mirror `effectiveDuration`, pass it to the lyric LLM, and pull the updated prompt .md files. Was: a 3:00 song got a ~20 s lyric. |
| **Preset delete/rename = inline icons** — the preset popup no longer has a "Delete a preset… (type the exact name)" entry. Each saved-preset row is now `[name (click = load)] [↺ rename] [✕ delete]`. Rename = client-side `get → save(newName) → delete(oldName)` (no new backend route); delete confirms. `popMenu` gained an `{el}` entry type for custom rows. | v1.24.x | `514d809` | node-verified 2026-09-06 (syntax) | user | Web twin: same UX — X = delete, ↺ = rename, no name-typing; reuse the existing `{save,delete,get}` preset routes for rename. |
| **OpenRouter reasoning-model `finish_reason=length` fix** — root cause of "웹은 OpenRouter가 막힌다": `config_music.json` `llm_or_model` = `deepseek/deepseek-v4-flash-0731` (reasoning model). Web sends the config value; node was sending `state.llmOrModel` (a non-reasoning model set in the UI while testing, never saved to config) — so node worked, web didn't. Server fix (`nodes.py`): `_openrouter_chat` split into `_openrouter_once()` + wrapper — first call `max_tokens≥8000` + `reasoning:{exclude:true}`; if content empty AND `finish_reason=="length"`, retry once with `max_tokens×2 (≥16000)` + reasoning **included**, then `_strip_thinking()` the monologue. | node `11eb126` | web `2f5f4ca` | **node-verified 2026-09-06** — `deepseek-v4-flash` → clean 2900-char caption / 40-line lyric via salvage | web→node | Web needs no code change (same route). Real fix = user picks a non-reasoning OpenRouter model in Settings so it lands in config. |
| **Queue stops on LLM failure** — `runLLM` gains `throwOnFail`; `llmOnce` passes it, so a caption/lyric LLM failure in `processJob` propagates → job card goes red "Failed" instead of silently rendering the raw brief. Title LLM stays best-effort (try/catch — audio exists by then). | node `5e289e9` | web `2f5f4ca` | node-verified 2026-09-06 (syntax) | web→node | Web already did this; node now aligned. User: "LLM 오류나면 큐가 넘어가면 안된다". |
| **Engine order: Ace-Step first + default** — `ENGINES` reordered (`acestep` before `minimax`); default engine `acestep` (core_music `defaultState`, graph_builder `commonMeta` fallback, backend `/music_one/config` GET). | node `5e289e9` | `066ea9c` | node-verified 2026-09-06; web browser-verified | user | Web twin: same reorder + default. |
| **Ace-Step sampling stages = sequential opt-in** — stage 1 always runs; stages 2 & 3 are checkboxes. Stage 3's checkbox disabled until stage 2 on; turning a stage off cascades off to all later stages. `aceStages[i].on` flag (default true, back-compat map in `defaultState`). `graph_builder` builds the `SamplerCustom` chain only from the enabled prefix (`allStages`→`stages`); `meta.aceStages` = full list, `meta.aceStagesRun` = active count. UI renders a checkbox per stage>1, `renderCompose()` on toggle, disabled step/cfg fields when inactive. | node `361fa24` | `066ea9c` | node-verified 2026-09-06 (syntax); web browser-verified (cascade off works) | user | Web twin: mirror — stage 1 fixed on, 2→3 sequential, filter the chain to the enabled prefix. |
| **Compose panel keeps its scroll position** — stage checkboxes / Add·remove LoRA / format select rebuild the compose panel via `renderCompose()`, which reset `scrollTop` to 0 (jump to top on every click). Capture `compose.scrollTop` before `clear()`, restore after `renderFixed()` (sync + `requestAnimationFrame`). | node `57e95a9` | web `2f5f4ca` | node-verified 2026-09-06 (syntax) | web→node | Web already had this (`2f5f4ca`); node now aligned. |
| **MiniMax H3: pick reference audio / Audio-Lock source from the MusicMaker playlist** — new `web/shared/ui_audio_gallery_picker.js` `openAudioGalleryPicker(onPick, copyApi="/minimax_h3_one")`: overlay lists `/music_one/playlist` (reads `/music_one/config` `save_subfolder` first), each row = cover + title + engine + length + inline `<audio>` preview. On pick it `POST <copyApi>/copy_to_input {filename, subfolder, type:"output"}` (the **existing generic handler** — no backend change) → file lands in `input/` with a unique name → `onPick(name)`. Wired into: the 3 reference-audio slots (`buildClipMediaSlots` `onPickFromGallery` was video-only; gate widened, 🎵 icon for audio) in `ui_images_minimax.js` (left panel) + `ui_prompt_edit_minimax.js` (per-clip override), and the Audio-Lock file picker in `one_node_minimax_h3.js` `audioFilePicker()` (new 🎵 button beside ⬆). | node `be7b401` | `d5fc02b` | node-verified 2026-09-06; web tsc-clean (routes live, e2e pending a click-through) | user | Web twin: mirror — audio picker sourced from the music playlist, copy-to-input on pick, same 3 ref-audio slots + Audio Lock. Web already has an image gallery picker pattern to follow. |
| **눈가리기 (sensitive-media blur) on every node gallery — one shared server-side list** — the per-tile 👁 toggle blurs a thumbnail; the hidden set is stored ONCE at `GET/POST /tj_shared/sensitive_media` → `NODE_DIR/sensitive_media.json` (gitignored). `{items:[...]}` replaces, `{key,on}` merge-safe toggles. `web/shared/ui_sensitive_media.js` = the node-side helper: route-backed, in-memory cache, optimistic write + revert, repaints every live tile on load/change; `attachSensitiveToggle(cell, media, key, corner, afterRender)`, `mediaKey(fn,sub)` = `"<subfolder>/<filename>"`, `isBlurred`, `setRevealAll`. Wired into krea2 / klein / qwen2511 / anima / sdxl / z-image image galleries + the MiniMax H3 clip gallery (H3 also blurs its hover-preview `<video>`) + the MusicMaker playlist rows (`f4541b4`). Route also accepts `{add:[...]}` to union-seed without clobbering (`6419392`). | node `f2386f9`+`6419392`+`f4541b4` | `8b5338b` | **node-verified live**; **web browser-verified 2026-09-06** — 38-item localStorage list auto-seeded to the server via `{add}` (migrated flag set), krea2 gallery renders exactly the 10 hidden items blurred, `{key,on}` toggle round-trips (38→39→38). localStorage kept only as a read-mirror. | user | **Web twin: switch `src/shared/sensitiveMedia.ts` from localStorage to this route.** Migration: first load POSTs `{add:[...old localStorage list]}` once (union), then localStorage flag `migrated:true`; after that GET `{items}` on load, POST `{key,on}` on toggle. Keep the session-only `revealAll` peek local. Same key format `"<subfolder>/<filename>"`. |
| **Image-node LLM panel gains an OpenRouter backend** — shared `web/shared/llm_panel.js` (Enhance + Image→Prompt tabs, used by krea2/sdxl/klein/z-image/qwen2511) now has a **Backend** select (Local GGUF / OpenRouter) in both tabs, an OR model picker (`/music_one/openrouter_models`), and a masked key field. Backend: `/tj_studio_one/llm/{enhance,image_to_prompt}` branch on `body.backend=="openrouter"` → `_openrouter_chat` / new `_openrouter_vision`; `/tj_studio_one/llm/models` returns `openrouter_key_hint` + `or_model` + `local_available`; new `POST /tj_studio_one/llm/config` writes the key to `.env` (SAME key as the music node) + `or_model` to `studio_llm.json` (gitignored). When TJ_NODE local LLM is absent the panel now falls back to OpenRouter instead of a dead "not installed" banner. | node `68064ac` | `1300355` | node-verified 2026-09-06; web browser-verified (backend toggle + OR model list render, GGUF rows hide under OpenRouter) | user | Web twin: mirror the Backend toggle in its prompt-enhance / image-to-prompt UI; key is server-side (`.env`), never in the browser; reuse `_openrouter_*`. |
| **MiniMax H3 Image→Brief gains an OpenRouter backend** — Settings → LLM tab: **LLM backend** = Native (ComfyUI CLIP) or OpenRouter. Under OpenRouter: OR model picker + masked key field (writes the shared `.env` key via `/tj_studio_one/llm/config`), CLIP pickers hidden. Backend routes `POST /minimax_h3_one/llm/analyze` (all ref images in one OpenRouter vision message — new `_openrouter_vision_multi`, reads from `input/`) and `POST /minimax_h3_one/llm/write_brief` (`_openrouter_chat`). `ui_prompt_edit` Enhance branches on `state.h3LlmBackend`; the "no CLIP set" guards relax under OpenRouter. `h3_llm_backend` / `h3_or_model` round-trip through `/minimax_h3_one/config`; defaults in `core_minimax.js`. | node `d1bc4d6` | `1300355` | node-verified 2026-09-06; web browser-verified (Settings LLM tab backend select renders) | user | Web twin: same backend toggle in H3's LLM settings; analyze/write_brief go through the node routes; key server-side. Installer already ships `ComfyUI-Openrouter_node` (REPOS[24], `.sh`, dependency_check). |
| **OpenRouter LLM fail-fast** — user hit "LLM: Failed to fetch" on MusicMaker lyrics: `config_music.json llm_or_model` was `deepseek/deepseek-v4-flash-0731` (a *reasoning* model — ~38s+ on a full lyric, minutes with retry), and the frontend proxy (Vite dev proxy / Cloudflare) reset the socket before the response → browser sees a raw `TypeError`. Node fix: `_openrouter_once` gains a `timeout` param (default 60s) catching `socket.timeout`/`URLError` → clean "OpenRouter timed out — pick a non-reasoning model" `RuntimeError`; `_openrouter_chat` first try floors max_tokens 8000→3000 @ 45s, one retry @ 45s (≤~95s total, so a readable 500 JSON always makes it back). Vision helpers 180/200→90/110s. | node `205b495` | `—` | node-verified 2026-09-07 (lyrics 38s→fail-fast path) | node | Web twin: N/A — all web LLM goes through the node's `/music_one/llm/run` + `/tj_studio_one/llm/*`. Web added its own defence anyway: `vite.config.ts` proxy `timeout`/`proxyTimeout` 600s + an `error` handler returning `504 {ok:false,stage:"proxy"}` instead of a dead socket; `src/tools/music/view.ts` `runLLM` maps "Failed to fetch" → "LLM 서버 응답 지연 — Settings에서 더 빠른 모델을 고르거나 재시도". Real fix is the user picking a non-reasoning model. |
| **OpenRouter `:free` model auto-fallback** — `_or_model_list()`: when `llm_or_model` ends in `:free`, the node sends `models: [free, paid-equivalent]` so OpenRouter routes to the paid tier server-side **only** on a free 429 / outage (one request, no added latency, paid billed only on fallback). Explicit `a|b|c` chains also supported. Applied in `_openrouter_once` + both vision helpers. User is on `minimax/minimax-m3:free` (node-measured 8s / ok). | node `d705e15` | `—` | node-verified 2026-09-07 | node | Web twin: N/A — model string is server-side config; web never sees it. ComfyUI restart applies `205b495` + `d705e15`. |
| **OpenRouter text/vision model split** — the Enhance / brief-writing call (text only) and the Image→Prompt / analyze call (reads pixels, needs multimodal) get **separate** OpenRouter model strings — cost differs (brief → cheap text model e.g. deepseek, vision → gemini / qwen-vl). One shared `.env` key. **Image 6 tools** (`studio_llm.json`): `or_model` → `or_model_text` + `or_model_vision` (old value migrates to both); `GET /tj_studio_one/llm/models` returns both (+ `or_model` = text for back-compat); `POST /tj_studio_one/llm/config` takes `or_model_text` / `or_model_vision`; `/llm/enhance` → text, `/llm/image_to_prompt` → vision (an explicit body `or_model` still wins). **MiniMax H3** (`config_minimax_h3.json`): `h3_or_model` → `h3_or_model_brief` + `h3_or_model_vision` (old → brief); `/llm/write_brief` → brief, `/llm/analyze` → vision; falls back to `studio_llm.json` text / vision when the H3 keys are unset. Default for both roles: `google/gemini-2.5-flash`, only when nothing valid is set. **Music unchanged** — caption / lyrics are text-only, `config_music.json llm_or_model` stays one string. | node `49422ca` | `7eedeff` | node-verified 2026-09-08; web tsc + build clean (browser e2e pending) | user | Web twin: `llmBackendPanel.ts` `createLlmBackendGroup` — `makeBlock(role)` binds text→`state.or_model`, vision→`state.or_model_vision`; enhance sends `or_model` (text), i2p sends `or_model_vision`; `loadModelsOnce` seeds both from `d.or_model_text` / `d.or_model_vision`; per-role config push (`or_model_text` / `or_model_vision`). H3 `settings.ts` OpenRouter section = 2 model pickers (brief / vision), `h3_or_model_brief` / `h3_or_model_vision` round-trip; `promptEdit.ts` analyze passes `h3OrModelVision`, write_brief passes `h3OrModel`; `h3OrModelVision` state field + default in `core.ts`. Applied to krea2 / klein / anima / zimage / qwen2511 / sdxl. |
| **…extended to per-role *backend*** — not just the model: the text (brief / enhance) and vision (analyze / i2p) roles pick backend **and** model independently — brief native / vision OpenRouter, or the reverse. **Image 6 tools**: `backend` → `backend_text` + `backend_vision` (legacy migrates); each `makeBlock(role)` Backend select drives its own key + `pushLlmConfig`; enhance body → `backend_text`, i2p → `backend_vision`. **MiniMax H3**: `h3LlmBackend` → `h3BriefBackend` + `h3VisionBackend`, `h3OrModel` → `h3OrModelBrief`; Settings LLM tab = two independent rows (Brief / Vision), each a Backend select + (native → CLIP picker \| openrouter → OR model select), shared key shown once; `promptEdit` `briefOR` / `visionOR` replace `useOR` — guards, progress label, analyze/write-brief branch all per-role; config `h3_brief_backend` / `h3_vision_backend` / `h3_or_model_brief` / `h3_or_model_vision`, falling back to the pre-split keys. | node `488bad2` + `b995d8d` | `aea37ce` | node-verified 2026-09-08; web tsc + build clean (browser e2e pending) | user | Web twin as above; music untouched. |
