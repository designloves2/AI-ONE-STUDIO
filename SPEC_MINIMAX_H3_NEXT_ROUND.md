# SPEC — MiniMax H3 큐 구조 개편 + 클립 메타데이터 확장 (ComfyUI 노드 버전 → 웹 버전 포팅)

이 문서는 `ComfyUI-TJ_NODE_STUDIO_ONE`(ComfyUI 커스텀 노드 버전, `web/one_node_minimax_h3.js`
등)에 **이미 구현 완료된** 두 묶음의 변경사항을 웹 버전(`AI-ONE-STUDIO`)에 포팅하기 위한
지시서다. 코드는 TypeScript(`src/tools/minimax_h3/*.ts`)로 옮겨 적는 것을 전제로 하되,
아래 설계 결정과 이유는 그대로 따라갈 것.

---

## Part 1 — 큐 구조 개편 (Next Gen 대기 큐 + 새로고침 복구 + 절전 방지)

### 1-1. 런 시작 시 전체 패널 스냅샷 고정 (컴피 큐와 동일한 방식)
- **이전**: 릴레이 루프가 매 클립마다 그 순간의 라이브 상태(state)를 다시 읽었음 —
  그래서 실행 도중 "아직 안 그려진 뒷 클립"의 프롬프트를 실시간으로 고칠 수 있었음.
- **지금**: Generate(또는 Next Gen) 클릭 시점에 패널 전체를 깊은 복사
  (`JSON.parse(JSON.stringify(state))`)해서 하나의 스냅샷(`rs`)으로 얼려두고, 이후 그
  런의 모든 클립은 이 스냅샷만 읽는다. 라이브 상태를 실행 중에 고쳐도 지금 도는 런에는
  전혀 영향 없음.
- **트레이드오프**: "실행 중 아직 안 그려진 뒷 클립 프롬프트를 실시간으로 수정하는"
  기능은 없어진다. 대신 이게 있어야 Next Gen을 안전하게 쓸 수 있다(아래 1-2).

### 1-2. Next Gen 대기 큐 — 클립 개수 제한 없이 항상 사용 가능
- 이전엔 "활성 프롬프트 1개일 때만 노출"이라는 제약이 있었는데, 위 1-1 스냅샷 고정
  덕분에 이 제약이 필요 없어졌다. **클립이 몇 개짜리 런이든 상관없이 Next Gen 버튼이
  항상 나타난다.**
- 클릭할 때마다 그 시점 패널 전체를 스냅샷 떠서 큐(배열)에 계속 추가 — 컴피 자체
  큐처럼 FIFO. 버튼에 "⏭ Next Gen (N)"으로 개수 표시.
- 큐가 1개 이상이면 "📋" 버튼이 나타나고, 클릭하면 팝업으로 대기 목록(순번 + 클립 수 +
  첫 프롬프트 요약)을 보여주고, 항목별 "✕ Cancel"로 개별 취소 가능.
- 현재 런이 **정상 종료**(에러/Stop 아님)되면 큐 맨 앞(#1)이 패널을 이어받아 자동
  시작. 그 다음 런이 끝나면 #2가 이어받는 식. **Stop을 누르면 큐 전체가 비워짐.**

### 1-3. 새로고침 후 릴레이 자동 복구
- 클립이 끝날 때마다 진행 상황을 저장: `pos`(다음에 만들 클립 위치), 활성 프롬프트
  인덱스 목록, `chainFrame`(Last Frame Chain용), One-Take `prevCheckpointName`, 완료된
  `clipRecords`, 그리고 **그 런이 쓰던 스냅샷(`runState`, 위 1-1의 `rs`) 자체**까지
  함께 저장 — 새로고침 후 이어갈 때도 라이브 상태가 아니라 이 저장된 스냅샷을 그대로
  다시 읽는다.
- 정상 종료 / 수동 Stop / 에러 — 이 세 경우엔 저장된 진행상황을 삭제. 브라우저를 그냥
  새로고침한 경우에는(그 삭제 코드를 거칠 기회가 없으므로) 저장값이 그대로 남아있고,
  이게 곧 "런이 실행 중이었다"는 신호가 된다.
- 페이지 로드 시 저장된 진행상황이 있는지 확인 →
  - 서버 큐에서 지금 실행 중인 클립(prompt_id)이 아직 있으면, 새로 제출하지 않고 그
    작업의 완료를 폴링(히스토리 엔드포인트)으로 기다렸다가(라이브 프리뷰도 자동으로
    다시 붙음) 이어서 다음 클립부터 정상 진행.
  - 없으면(이미 끝났거나 서버가 재시작됨) 저장된 위치의 클립부터 새로 빌드해서
    이어서 진행.
- 결과적으로 멀티클립 런 도중 새로고침해도 처음부터 다시 시작할 필요 없이, 남은
  클립들과 (해당되면) One-Take 스티치까지 자동으로 이어서 완료된다.

### 1-4. 절전모드 / 탭 종료 방지
- **웨이크 오디오**: 실행 중(Next Gen 연쇄 포함)에는 거의 무음에 가까운(볼륨 매우
  낮지만 0은 아님) 오디오를 Web Audio API로 계속 재생해, 브라우저/OS가 탭을 절전
  상태로 넘기지 않도록 유도 — **웹 버전에 이미 이 우회책이 있다고 확인했으니 참고만.**
  볼륨을 완전히 0으로 두거나 `muted`로 하면 브라우저의 절전 휴리스틱이 오히려 그걸
  기준으로 탭을 덜 활성으로 판단하니 주의.
- **탭 종료 경고**: 이 노드/툴 자신의 릴레이(`running`) 또는 Next Gen 대기열이 하나라도
  있을 때만 `beforeunload`로 브라우저 네이티브 "이 페이지를 나가시겠습니까?" 경고
  표시. 노드가 선택돼 있는지와 무관, 다른 노드/툴/큐에는 영향 없이 **자기 자신의 큐
  상태만** 보고 판단.

### 1-5. README 안내 (참고 — 웹 버전에도 동일 취지로 안내 문구 필요할 수 있음)
- 이미지 생성(RUN 큐)은 서버가 그래프 전체를 통째로 소유해서 브라우저를 닫아도 계속
  처리되지만, MiniMax H3 클립 릴레이는 **다음 클립의 그래프를 "이전 클립의 실제 결과"를
  보고 그때그때 만들기 때문에** 미리 통째로 제출해둘 수 없고, 그 판단을 브라우저 쪽
  JS가 매 클립마다 내린다. 그래서 지금 실행 중인 클립 하나는 브라우저를 닫아도 서버에서
  끝까지 돌지만, 그다음 클립을 이어서 큐에 넣어줄 코드가 사라져 거기서 멈춘다. 컴퓨터/
  모니터 절전모드도 탭을 닫는 것과 같은 문제. 새로고침은 안전(1-3이 이어받음).

---

## Part 2 — 클립 메타데이터 확장 + 완전 Reuse + 실측 평균 소요시간

`SPEC_CLIP_METADATA_REUSE.md`(이 저장소에 이미 있는 원본 스펙 문서)와 동일한 내용을
**ComfyUI 노드 버전에 먼저 구현 완료**했다. 웹 버전으로 포팅할 때는 그 스펙 문서를
그대로 따라가되, 아래 실제 구현에서 내린 결정들을 참고할 것 (스펙 문서에 "결정 필요"로
남아있던 부분들에 대한 답):

- **일반 LoRA 슬롯**: 노드 버전은 이미 `state.loras`가 `{name, strength, triggerWord,
  enabled}` 배열로 존재해서 별도 작업이 필요 없었음. 웹 버전 쪽 `LoraEntry`도 이미 같은
  모양이라면(스펙 문서에 나온 그대로) 마찬가지로 작업 A는 건너뛰어도 됨 — `state.loras`
  구조가 실제로 스펙과 일치하는지만 확인.
- **elapsedSec 캡처 시점**: 클립의 `res`(큐 결과)를 받은 직후, 다음 클립으로 넘어가기
  전 마지막 프레임 후처리(체인 프레임 복사 등) 시작 전에 `Date.now() - clipStart`로
  계산 — 후처리 오버헤드는 포함하지 않음(모델이 실제로 렌더링한 시간에 더 가깝게).
- **meta에 추가한 필드**: `elapsedSec`, `turboLora`, `turboLoraReference`,
  `turboLoraStrength`, `turboLoraLowVram`, `loras`(스냅샷 배열, `enabled:false`인 것도
  그대로 포함).
- **갤러리 ⓘ 아이콘**: 썸네일 카드 좌상단(삭제 버튼은 우상단)에 배치, 마우스오버 시
  작은 고정(fixed) 위치 팝업으로 해상도/프레임수/스텝/샘플러/가속모드/소요시간(mm:ss)/
  터보 LoRA(이름+강도)/일반 LoRA 목록(이름+강도+on-off)/시드 표시. 저장된 meta가 없는
  옛날 클립은 "No settings saved for this clip." 표시.
- **완전 Reuse — 시드 처리 결정**: 스펙 문서가 "버튼을 두 개로 나눌지 결정 필요"라고
  남겨둔 부분 → **버튼 하나로 통일, 항상 같은 시드로 복원**하고 `seedMode`도
  `"fixed"`로 같이 바꿔줌(다음 Generate에서 재랜덤되지 않도록). 스펙 배경 자체가 "한
  번 잘 나온 클립을 그대로 다시 만들기"이므로 완전 재현이 기본값. 다른 시드로 변주하고
  싶으면 사용자가 Reuse 후 수동으로 시드 모드를 바꾸면 됨 — 별도 "new seed" 버튼은
  만들지 않았음. (웹 버전에서 두 버튼을 원하면 그렇게 가도 되지만, 노드 버전과의 UX
  일관성을 위해 하나로 맞추는 걸 권장.)
- **Reuse가 실제로 복원하는 필드**: 프롬프트(+헤더/풋터, 기존 기능) 외에 `aspect`,
  `megapixels`, `clipFrames`(meta의 `frames`), `steps`, `sampler`,
  `accelMode`(meta의 `accel`), `seed`+`seedMode`, `turboLora`,
  `turboLoraReference`, `turboLoraStrength`, `turboLoraLowVram`, `loras`. **meta에 없는
  필드는 조용히 스킵**(현재 패널 값을 건드리지 않음) — 옛날 클립(이 스펙 적용 전에
  저장된 것)을 reuse해도 에러 없이 있는 것만 복원됨.
- **실측 평균 소요시간 — 매칭 기준**: 저장 폴더의 과거 클립들 중 **현재 패널 설정과
  다음이 전부 일치**하는 것만 표본으로 사용:
  `aspect` 동일, `megapixels` 거의 동일(오차 0.01 이내), `frames`(clipFrames) 동일,
  `accel`(accelMode) 동일, "LoRA 사용 여부"(활성화된 LoRA가 1개라도 있는지, boolean만
  비교 — 정확한 이름/강도까지는 안 봄) 동일. 터보 LoRA는 `accel === "turbo"` 비교에
  이미 포함되므로 별도 체크 불필요.
  일치 표본이 있으면 평균(`elapsedSec` 합 ÷ 개수 ÷ 60 = 분)을 Settings의 "Avg minutes
  per clip" 필드에 자동 반영(수동 입력 필드는 그대로 편집 가능하게 유지)하고, 옆에
  "Measured from N matching clips" 안내 표시. 일치 표본이 없으면 "No past clips at the
  current settings yet — using the manual value above." 표시하고 기존 수동값 유지.
  Settings 화면이 열릴 때마다(또는 Output/Relay 탭이 렌더될 때마다) 한 번 조회.

### 영향 파일 (노드 버전 기준 — 웹 버전은 해당 TS 파일로 매핑)
- `web/one_node_minimax_h3.js` → `src/tools/minimax_h3/view.ts` — 클립 타이밍 캡처,
  meta 확장, `reusePrompt`/`reuseAll`(신규), Next Gen 큐, 스냅샷 고정, 새로고침 복구,
  웨이크 오디오, `beforeunload`
- `web/minimax/ui_gallery_minimax.js` → `src/tools/minimax_h3/galleryOverlay.ts` — ⓘ
  아이콘 + 호버 팝업, Reuse 버튼이 `reuseAll` 호출하도록 변경
- `web/minimax/ui_app_settings_minimax.js` → `src/tools/minimax_h3/settings.ts` — 실측
  평균 소요시간 조회/표시
- `web/minimax/api_minimax.js` → 해당 API 클라이언트 — `waitForHistory`류(이미 큐에
  있는 prompt_id의 완료를 폴링으로 기다리는 함수) 신규 필요

### 주의사항
- Part 1, Part 2 모두 **이 변경 적용 이후 생성되는 클립부터만** 새 메타 필드가 쌓임.
  기존 클립은 `elapsedSec`/`loras`/`turboLora*`/`_relay` 관련 필드가 없어도 에러 없이
  그냥 스킵되도록 방어적으로 짤 것(옵셔널 체이닝 / null 체크).
